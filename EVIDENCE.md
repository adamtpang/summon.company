# EVIDENCE: summon.company

Numbers only. A zero is data and gets logged as a zero. No gate passes without a
number or a dated receipt.

## Baseline (as of 2026-08-01, from live Stripe and live HTTP reads)

| Metric | Value | Source | As-of |
| --- | --- | --- | --- |
| Revenue (stranger $) | **$0** for Summon | Stripe API | 2026-08-01 |
| MRR | **$0** | Stripe API | 2026-08-01 |
| Subscriptions, all statuses, ever | **0** | Stripe API, `subscriptions?status=all` returned 0 | 2026-08-01 |
| Active customers | **0** | Stripe | 2026-08-01 |
| Named verbal yes, never sent a link | **1** (Anton, Regain Inc) | ANTON_GIVE.md | 2026-08-01 |
| Warm second, never contacted | **1** (Michael, Hawaii Tech Week) | company/sales/FOUNDING-PIPELINE-LEDGER.md | 2026-08-01 |
| Public rail on the site | **1**, live and verified | buy.stripe.com/8x2eVd1ACfJb5kc1q9aMU19 | 2026-08-01 |
| Weekly active usage | Company Zero dogfood only | control plane | 2026-08-01 |
| Time to first value | 60 seconds via /diagnose, unproven on a stranger | verified working, see below | 2026-08-01 |
| Top risk | **The ask has never been sent.** Product risk is second to that. | this file | 2026-08-01 |

## What was actually broken on 2026-08-01, found by checking

The brief said the public face was unclear because `.vercel` pointed at the
wrong project. That was wrong on both counts, and the real problem was worse.

| Claim in the brief | What was actually true |
| --- | --- |
| `.vercel` wrongly points at vitals.run | **Correct as-is.** `vercel domains inspect summon.company` shows the Vercel project named `vitals.run` serves both summon.company and design.summon.company. Only the project's display name is stale, from the pre-Summon rename. See PUBLIC_FACE.md. |
| The Stripe rail is live and correct | True of the rail, but **the rail was not on the site.** The live page carried 8 buy buttons and **all 8 were dead**: 8 of 8 payment links inactive, 8 of 8 prices archived. summon.company could not take a single dollar. |
| n/a | The live page also sold a **superseded $99/mo offer** and shipped JSON-LD to search engines quoting `"price":"99.00"`. |

## The rail, verified 2026-08-01

Checked against the Stripe API and rendered in a browser, not assumed.

| Object | ID | State |
| --- | --- | --- |
| Public product | `prod_Uvlz1aAKZ86BVm` Summon Founding Seat | active |
| Public price | `price_1TvuRZFL7C10dNyGWfMlpC7b` | active, $500/mo, nickname "Summon Founding Seat - $500/mo", `keep: true` |
| Public buy URL | https://buy.stripe.com/8x2eVd1ACfJb5kc1q9aMU19 | renders "Subscribe to Summon Founding Seat", SGD 667.04/mo at 1 USD = 1.3341 SGD |
| Anton setup | `price_1TzWS2FL7C10dNyGIixanuvB` | active, $500 one-time, labeled, `keep: true` |
| Anton monthly | `price_1TzWSKFL7C10dNyGYNNcZnku` | active, $99/mo, labeled, `keep: true` |

**Duplicate resolved.** A second founding rail with identical economics
(`prod_UzZ8krCO3LnwKC`, $500 once + $99/mo on one link
`6oUaEX2EG2Wp9Asgl3aMU1g`) was minted hours after the pair above. Board decision
2026-08-01: keep the two-link pair that `outbound/anton-founding-send.md`
actually references, archive the single-link version. Product, both prices, and
the payment link are now `active: false`. Three active Summon products remain,
one price each.

## Verified results log

| Date | Change | Before | After | Evidence |
| --- | --- | --- | --- | --- |
| 2026-07-26 | Fleet bootstrap created this baseline | n/a | file exists | this file |
| 2026-07-31 | The Anton give: fixed surviving P0-9 in regain/miss | 16 tests, bug live on main | 19 pass 0 fail, tsc clean, +70/-6 on a local branch, never pushed | ANTON_GIVE.md, anton-give.diff, commit dc24ee54c |
| 2026-08-01 | Re-verified the Anton give on the real repo | claimed done | branch `fix/exec-alerts-dataasof-watermark` exists, `dc24ee54c` is 2 files +70/-6, `git branch -r --contains` empty so their remote is untouched | regain/miss |
| 2026-08-01 | OFFER.md filled for real, no brackets | template with `[buyer]` | real buyer, pain, cure, alternative, proof, price, risk reversal | OFFER.md |
| 2026-08-01 | Public face decided and documented | ambiguous, agents kept "fixing" a correct .vercel link | explicit decision recorded, landing stays in apps/landing under the vitals.run project | PUBLIC_FACE.md |
| 2026-08-01 | Landing rewired to the live rail | 8 dead buy buttons, $99/mo copy, JSON-LD price 99.00 | 1 live link in 4 CTAs, $500/mo founding seat, JSON-LD price 500.00 | apps/landing/index.html |
| 2026-08-01 | Stale price copy swept across the funnel | $99/mo on diagnose, blog, kit, and 9 generated role pages | all updated, generator `build-roles.mjs` fixed at source and roles regenerated | apps/landing |
| 2026-08-01 | Stripe duplicate founding rail archived | 2 competing founding offers | 1 archived, 3 active products with 1 price each, all labeled `keep: true` | Stripe API |
| 2026-08-01 | Onboarding written for the first 30 minutes | none existed | ONBOARDING.md, with the verified zero-install path first | ONBOARDING.md |
| 2026-08-01 | /diagnose verified working end to end | unverified | POST returned stage 7, a named binding constraint, a precedent, and a first move for a test business | live API |
| 2026-08-01 | Fleet ring added to the landing footer | none | hub plus everybot.fun and summon.guide, renders at 375px with no overflow | apps/landing/index.html |
| 2026-08-01 | fleet.json status corrected | "code" | "live", the site returns HTTP 200 | Aether/fleet.json |
| 2026-08-01 | Deployed to production on Adam's instruction | live site had 8 dead buy buttons and sold a superseded $99/mo offer | live site sells the $500/mo founding seat through the one working rail. 14 mentions of $500, zero of $99/mo, JSON-LD price 500.00. design.summon.company, vitals.run, /diagnose, /roles/, /blog.html, /changelog.html, /terms.html all still 200 | deploy `dpl_9oJyZUGxYouc36MwHZChH8cNwjwe` |
| 2026-08-01 | Michael follow-up drafted | none | outbound/michael-followup.md, unsent, with the Haven precondition flagged | outbound/ |
| 2026-08-25 | Polsia P0 company loop implemented in source | No bounded Nightshift, company-level next-three action, or daily company brief | 1–12 hour durable Nightshift, safe stop, Cofounder next-three proposal, daily + terminal briefs; 10 UI tests and 7 server tests pass, shared/server/UI typechecks pass, UI production build passes; not deployed or canaried | `server/src/services/company-loop.ts`, `ui/src/components/CompanyLoop.tsx`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md` |
| 2026-08-25 | Polsia proposal-order parity implemented in source | Cofounder task proposals could only be selected or rejected; accepting silently restored payload order | Drag, keyboard, and arrow reordering plus clear/reset selection; server preserves board-selected task order during creation; 26 UI tests and 3 server tests pass, shared/server/UI typechecks and UI production build pass; token gate remains at 102 unrelated pre-existing violations | `ui/src/components/IssueThreadInteractionCard.tsx`, `server/src/services/issue-thread-interactions.ts`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md` |
| 2026-08-26 | Durable accepted-work dispatch rail implemented in source | Accepted tasks fell back to status/priority time ordering and could not be persistently reprioritized as one company queue | Accepted proposals and later board reorder events project into one append-only, company-scoped queue with optimistic revisions; Mission Control exposes accessible drag/keyboard/arrow ordering and Nightshift consumes the first ready task; 27 focused UI tests and 10 focused server tests pass, shared/server/UI typechecks and UI production build pass; token gate remains at 102 unrelated pre-existing violations, with zero in this slice | `server/src/services/company-loop.ts`, `ui/src/components/CompanyLoop.tsx`, `ui/src/pages/DesignGuide.tsx`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md` |
| 2026-08-26 | Evidence-driven company office embedded in Mission Control | The default dashboard repeated eight flat Org cards while the business-simulation view lived behind a separate primary navigation item | Mission Control now contains a fixed floor plan for the board, Cofounder, and eight departments; every room derives state from assignments, heartbeat runs, issue locks, review/blocker state, and recent shipped work, then links to the real task, employee, decision deck, or staffing flow. The duplicate Org grid and Factory Floor sidebar entry are removed; desktop and mobile Storybook screenshots reviewed; 32 focused UI tests, UI typecheck, production build, and static Storybook build pass; token gate remains at the same 102 unrelated violations with zero in this slice | `ui/src/components/CompanyOffice.tsx`, `ui/src/components/MissionControl.tsx`, `ui/storybook/stories/company-office.stories.tsx`, `work/company-office-desktop.png`, `work/company-office-mobile.png` |
| 2026-08-26 | Polsia P0 proved in an isolated packaged-runtime canary | Source tests and previews did not prove that the publish payload could install, migrate a fresh database, serve the current UI, or expose Company Loop on Windows | A task-owned install of 17 locally packed runtime tarballs applied all 152 migrations through `0153`, returned health `status: ok`, `bootstrapStatus: ready`, and `authReady: true`, served the branded root and embedded Company Office, and returned the durable queue plus deterministic brief from `GET /api/companies/:companyId/company-loop`. The canary exposed and fixed a missing `0153` journal entry, Windows path normalization in the release package map, and stale nested migration payloads in repeated DB builds. Rollback closed ports 3114 and 54329; Ctrl-C still logged a Windows process-group `ECONNRESET` while draining heartbeat runs, so the existing packaged-database cutover remains a separate S0 gate. | `packages/db/package.json`, `packages/db/src/migrations/meta/_journal.json`, `scripts/release-package-map.mjs`, `work/polsia-parity-canary-20260826/company-office-packaged-canary-floor.png` |
| 2026-08-26 | Polsia P1 Company Inbox core implemented and source-canaried | Inbox was work-centric, Messages was an employee-chat prototype, and no one contract declared customer-source scope, freshness, reply authority, revocation, or evidence | Added 2 durable company-scoped tables and migration `0154`; 8 company-inbox API paths; idempotent inbound ingestion; unread/read/archive state; accountable owners; scope enforcement; revocation; activity receipts; and reply drafts that cannot send. A fresh isolated runtime applied all 153 migrations through `0154`, returned health/bootstrap/auth ready, and rendered 2 declared sources plus 3 messages. Desktop and mobile captures measured zero horizontal overflow at 1440px and 390px. 2 focused server tests and 1 focused UI test pass; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. Rollback closed ports 3116 and 54329; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. Live Gmail/Slack authorization, provider sync, and approved-send receipts remain unclaimed. | `doc/COMPANY-INBOX.md`, `server/src/services/company-inbox.ts`, `ui/src/pages/CustomerInbox.tsx`, `work/company-inbox-canary-20260826/customer-inbox-desktop.png`, `work/company-inbox-canary-20260826/customer-inbox-mobile.png` |
| 2026-08-26 | Governed Gmail sync and customer-reply execution implemented and source-canaried | Company Inbox could declare Gmail-shaped records and save drafts but could not verify a mailbox, synchronize provider messages, request/send an approved reply, reconcile provider ambiguity, or show provider receipts | Migration `0163` adds exact secret, approval, provider-operation, sent/error state, and append-only Inbox receipts. The fixed-origin Gmail adapter verifies the declared account, reads at most 25 Inbox messages newer than 30 days, sends one safe base64url RFC 2822 reply into the existing thread, and stores only allowlisted evidence. Board sends require an exact-draft confirmation; employee sends bind recipient plus content hashes to `outbound_message` and `customer_data_access` approval and resume only through the same owner. Every call reserves execution first; definite rejection and ambiguous outcome are terminal and automatic replay is blocked. Eleven focused tests pass across validation, provider normalization/MIME/redaction, embedded-database custody/authorization/approval/idempotency/failure, and UI confirmation. Shared/DB/server/UI typechecks and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in this slice. An isolated migration-current runtime applied `0163`, proved one exact `system / provider.token` binding, rendered connected/synced/received/drafted/reserved/submitted receipts plus the irreversible send dialog with zero horizontal overflow at 1440px and 390px, and made zero live Gmail calls. Rollback closed ports 3130/54342; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. Live credential acceptance, OAuth/token refresh, push/webhook sync, stale reservation reconciliation, notifications, reply-to-work conversion, and Slack remain unclaimed. | `doc/COMPANY-INBOX.md`, `server/src/services/gmail-company-inbox-provider.ts`, `server/src/services/company-inbox.ts`, `server/src/__tests__/company-inbox.test.ts`, `ui/src/pages/CustomerInbox.tsx`, `work/gmail-inbox-canary-20260826/gmail-inbox-desktop.png`, `work/gmail-inbox-canary-20260826/gmail-inbox-mobile.png`, `work/gmail-inbox-canary-20260826/gmail-send-confirm-desktop.png` |
| 2026-08-26 | Governed Slack company channel implemented and source-canaried | Slack was only a declared source label; Summon could not verify exact workspace/channel authority, ingest bounded human channel history, reply in one thread, or surface company-channel state in Mission Control | Added a fixed-origin Slack adapter under the existing Company Inbox contract. One connector binds exactly one stable `TEAM_ID:CHANNEL_ID`; `auth.test` and `conversations.info` reject workspace mismatch, private, archived, shared/Slack Connect, or non-member channels; required provider scopes are only `channels:read`, `channels:history`, and `chat:write`; history is capped at 15 and excludes bot/self/subtype/system messages; outbound posts only as the app bot to the bound channel/thread and carries the Summon message UUID as `client_msg_id`. The Customers surface now registers, syncs, and sends Slack with the existing board/approval/reservation/receipt contract, while Mission Control shows governed source count, unread work, provider state, and exact channel label. Twenty-one focused tests across six files pass; shared/server/UI typechecks and the UI production build pass; the token gate remains exactly 102 unrelated pre-existing violations with zero in changed files. The corrected isolated runtime applied all migrations through `0164`, proved one exact `system / provider.token` binding, rendered connected/synced/received/drafted/reserved/submitted/failed receipts, the irreversible send dialog, and the Customer channels glance at 1440px and 390px with zero horizontal overflow. All canary ports are closed and zero live Slack calls were made. Live credential acceptance, OAuth/token refresh, Events API delivery, notifications, reply-to-work conversion, and stale-reservation reconciliation remain unclaimed. | `doc/COMPANY-INBOX.md`, `server/src/services/slack-company-inbox-provider.ts`, `server/src/services/slack-company-inbox-provider.test.ts`, `server/src/__tests__/company-inbox.test.ts`, `ui/src/pages/CustomerInbox.tsx`, `ui/src/components/MissionControl.tsx`, `work/slack-inbox-canary-20260826/slack-inbox-desktop.png`, `work/slack-inbox-canary-20260826/slack-inbox-mobile.png`, `work/slack-inbox-canary-20260826/slack-send-confirm-desktop.png`, `work/slack-inbox-canary-20260826/slack-mission-control-desktop.png` |
| 2026-08-26 | Slack canary configuration fallback mutated the default embedded database; stopped and preserved for operator inspection | The first Slack canary config used invalid `$meta.source` and logging enum values. Config parsing returned no usable config, so the source server silently fell back to `C:\Users\adamp\.paperclip\instances\default\db` instead of the intended task-local runtime | Before shutdown, the fallback runtime applied 11 pending source migrations `0154` through `0164` and startup reconciliation reported 99 agents scanned, 9 reconciled/auto-ensured, 1 pending approval, and 30 default grants ensured. It was stopped immediately. No rollback or restore was attempted; the newest observed pre-existing backup is `C:\Users\adamp\.paperclip\instances\default\data\backups\paperclip-20260821-165919.sql.gz`. The config was corrected and schema-validated before the successful task-local rerun. Ports 3100, 3132, 54329, and 54331 are closed. The default database must be inspected for packaged/source migration-lineage compatibility before the main control plane is restarted. | `work/slack-inbox-canary-20260826/runtime/config.json`, `EVIDENCE.md` |
| 2026-08-26 | Inbound customer message → accountable work loop implemented and migration-tested | Gmail and Slack messages could be read and answered, but customer demand remained isolated from Summon's owned task, wake, office, and execution lifecycle | Added migration `0165`, the separate `work.create` connector capability, one canonical message-to-issue foreign key, a unique `customer_message` task origin, and one unique `work_created` receipt. Only an inbound message may convert; the board or exact connector owner may do it; one active same-company owner is mandatory. The task is assigned `todo` to that owner and receives one assignment wake. Retries after a successful enqueue do not wake twice; a deliberate failed wake left the receipt resumable, and the retry queued the same task without creating another. The task description links the exact-message API and Customers URL but does not copy the message body or provider identifiers; customer replies remain inside Inbox approval/send governance. Customers projects live task title/status/priority back onto the canonical message, and Mission Control counts open customer tasks. Twenty-four focused tests across six files pass, including migration-current embedded PostgreSQL authorization, exact-message access, origin/link/receipt uniqueness, body non-copy, activity, wake recovery, idempotency, and UI projection. Shared/DB/server/UI typechecks, migration numbering/safety, and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with none in changed files. No default runtime was started and no provider call was made for this slice. | `packages/db/src/migrations/0165_company_inbox_work.sql`, `server/src/services/company-inbox.ts`, `server/src/routes/company-inbox.ts`, `server/src/__tests__/company-inbox.test.ts`, `ui/src/pages/CustomerInbox.tsx`, `ui/src/components/MissionControl.tsx`, `doc/COMPANY-INBOX.md` |
| 2026-08-26 | Polsia P1 Company Website control implemented and source-canaried | Summon had coding workspaces and deploy-capable agents but no company-level contract tying a public site to provider identity, accountable authority, deployment/domain/health state, or receipts | Added 2 company-scoped tables and migration `0155`; 8 Website API paths; exact scopes; observe-only or approval-required authority; board-only connect/revoke; owner-only production deploy proposals through the existing approval gate; deployment/domain provider reports; append-only receipts; and an SSRF-hardened public health probe. A fresh isolated runtime applied all 154 migration files through `0155`, reported a ready deployment and verified domain, and measured `https://summon.company/` at HTTP 200 in 109 ms. The Website page and Mission Control glance rendered with zero horizontal overflow at 1440px and 390px. 5 focused server tests and 3 focused UI tests pass; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. Rollback closed ports 3118 and 54329. Provider authorization, deploy execution/resumption, infrastructure provisioning, and DNS automation remain unclaimed. | `doc/COMPANY-WEBSITE.md`, `server/src/services/company-website.ts`, `server/src/services/public-website-probe.ts`, `ui/src/pages/CompanyWebsite.tsx`, `work/company-website-canary-20260826/company-website-desktop.png`, `work/company-website-canary-20260826/company-website-mobile.png`, `work/company-website-canary-20260826/mission-control-website.png` |
| 2026-08-26 | Governed Vercel + GitHub production execution implemented in source | Website could propose a board-gated deployment and accept external reports, but no installed adapter could resolve a credential, resume the approved action, call the provider, or prove retry and failure semantics | Added a ninth Website API path and a Vercel production adapter for an existing GitHub-backed project. Board setup binds one active company secret plus non-secret project/repository configuration; execution is restricted to the accountable owner, revalidates the exact approved Website and target ref, records secret access, reserves one approval-correlated receipt, calls only Vercel's fixed API origin, and stores allowlisted success or redacted failure evidence. Retries return the reserved receipt without a second provider call. Three shared validation tests, eight server/provider tests, and three UI tests pass; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. A fresh isolated runtime applied all 154 migrations, proved the exact `system` / `provider.token` binding, measured the public site at HTTP 200 in 170 ms, and rendered the managed-adapter dialog at desktop and mobile sizes, including reachable mobile actions. Rollback closed ports 3118 and 54329. No live Vercel deployment was attempted without an explicitly authorized credential; project/database provisioning, stale-reservation reconciliation, OAuth, and DNS automation remain unclaimed. | `server/src/services/vercel-deployment-provider.ts`, `server/src/services/company-website.ts`, `server/src/__tests__/company-website.test.ts`, `ui/src/pages/CompanyWebsite.tsx`, `doc/COMPANY-WEBSITE.md`, `work/company-website-canary-20260826/company-website-connect-dialog.png`, `work/company-website-canary-20260826/company-website-connect-dialog-mobile.png` |
| 2026-08-26 | Owner-controlled Company Payments core implemented and source-canaried | Summon had platform billing and ad hoc Stripe offer work, but no customer-company rail tying account custody, restricted credential authority, public prices, revenue evidence, accountable ownership, approvals, and receipts into one product contract | Added 3 company-scoped tables and migration `0156`; 10 payment API paths; a restricted-key-only Stripe adapter; one-time and recurring Payment Links; same-currency recent revenue and active-subscription evidence with an explicit latest-100 denominator; exact secret binding; board-only connection/direct publish/deactivation/revocation; and accountable-owner proposals gated as `public_claim` plus `contract`. Ten focused tests pass across shared validators, provider requests, embedded-database authorization/idempotency, and UI; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. A fresh isolated runtime applied all 155 migrations through `0156`, returned health/bootstrap/auth ready, proved one exact `system` / `provider.token` binding, and rendered the Payments page, Mission Control glance, and create dialog with zero horizontal overflow at 1440px and 390px. Rollback closed ports 3120 and 54332; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. No live Stripe call or customer charge was attempted without an explicitly authorized restricted key; OAuth/Connect onboarding, webhooks, fulfillment, refunds/disputes, tax workflows, and stale-reservation reconciliation remain unclaimed. | `doc/COMPANY-PAYMENTS.md`, `server/src/services/company-payments.ts`, `server/src/services/stripe-company-payments-provider.ts`, `server/src/__tests__/company-payments.test.ts`, `ui/src/pages/CompanyPayments.tsx`, `work/company-payments-canary-20260826/company-payments-desktop.png`, `work/company-payments-canary-20260826/company-payments-mobile.png`, `work/company-payments-canary-20260826/mission-control-payments.png` |
| 2026-08-29 | Stripe connection reduced from a Secrets detour to one governed action | Polsia's authenticated Business panel exposes one Setup payments action; Summon's source already had stronger company-owned Stripe evidence but required the founder to leave Payments, create a secret, return, and select it | The Payments dialog now selects Finance, accepts one restricted key directly into encrypted company custody, clears the field immediately, and passes only the secret ID to the existing connection service. Existing secrets remain selectable. Deterministic provider rejection cleans up a just-created invalid secret; ambiguous failure retains it encrypted for explicit retry. Full secret keys and broader refund authority remain rejected/separate. Three focused UI assertions, UI typecheck, production build, inherited 52/32/18 token baseline with no Payments finding, and Helium desktop/390×844 acceptance pass. The isolated 3109/54339 canary closed and the unrelated process on 3100 was untouched. No key was entered and no Stripe call, payment, refund, spend, customer mutation, or deployment occurred. | `doc/COMPANY-PAYMENTS.md`, `ui/src/pages/CompanyPayments.tsx`, `ui/src/pages/CompanyPayments.test.tsx`, `C:/Users/adamp/Aether/helium-harness/summon-stripe-direct-connect-desktop-20260829.png`, `C:/Users/adamp/Aether/helium-harness/summon-stripe-direct-connect-mobile-20260829.png` |
| 2026-08-29 | Plaid connection reduced from a Secrets detour to one governed action | Summon already exceeded Polsia's visible finance truth with continuous cash, posted expense, profitability, runway, and valuation inputs, but connecting a bank forced the founder to leave Finance, create two secrets, return, and select both | The Finance dialog now accepts the Plaid client ID and environment secret directly, encrypts each as a company secret, clears both browser fields before requesting Hosted Link, and supplies only secret IDs to the existing board-only service. Existing secrets remain selectable. Definite pre-connection rejection cleans up only the just-created credentials; provider/network ambiguity retains them because a durable connection may already exist. Five focused UI tests, UI typecheck, production build, inherited 52/32/18 token baseline with no finance-connection finding, and Helium desktop/390×844 top/action acceptance pass. The isolated 3109/54339 canary returned health `ok` with zero finance connections, then closed; unrelated PID 16764 on 3100 was untouched. No credential was entered and no Plaid call, Item, bank read, money movement, spend, or deployment occurred. | `doc/finance/PLAID-CONNECTION.md`, `ui/src/components/CompanyFinanceConnections.tsx`, `ui/src/components/CompanyFinanceConnections.test.tsx`, `C:/Users/adamp/Aether/helium-harness/summon-plaid-direct-connect-desktop-20260829.png`, `C:/Users/adamp/Aether/helium-harness/summon-plaid-direct-connect-mobile-20260829.png`, `C:/Users/adamp/Aether/helium-harness/summon-plaid-direct-connect-mobile-actions-20260829.png` |
| 2026-08-26 | Governed Company Social core implemented and source-canaried | Summon had no company-level contract tying a multi-network workspace, exact external identities, accountable ownership, immutable approved copy, scheduling, cancellation, provider results, and receipts into one governed rail | Added 4 company-scoped tables and migration `0157`; 12 Social API paths; one exact Zernio profile and company-secret binding; projected channels; immediate/future text posts; provider refresh; scheduled cancellation; board-only direct publication/revocation; and accountable-owner proposals gated as `public_claim` plus `outbound_message`. Eight focused tests pass across shared validators, provider requests, embedded-database authorization/idempotency, and UI; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. A fresh isolated source runtime applied all 156 migrations through `0157`, returned health/bootstrap/auth ready, proved one exact `system` / `provider.token` binding, rendered two synthetic channels plus one scheduled and one published post, and measured zero horizontal overflow at 1440px and 390px across the Social page, Mission Control glance, and create dialog. Rollback closed ports 3122 and 54334; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. No live Zernio or social-network call was made without an explicitly authorized profile key; media, per-network copy, analytics, replies/DMs, webhooks, published deletion, and stale-reservation recovery remain unclaimed. | `doc/COMPANY-SOCIAL.md`, `server/src/services/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/__tests__/company-social.test.ts`, `ui/src/pages/CompanySocial.tsx`, `work/company-social-canary-20260826/company-social-desktop.png`, `work/company-social-canary-20260826/company-social-mobile.png`, `work/company-social-canary-20260826/mission-control-social.png` |
| 2026-08-26 | Permission-based Company Outreach core implemented and source-canaried | Summon had AI SDR dogfood but no general company rail binding recipient authority, accountable ownership, exact copy, volume/follow-up ceilings, suppression, reply handling, provider evidence, and approvals | Added 6 company-scoped tables and migration `0158`; 16 Outreach API paths; one exact Postmark server and company-secret binding; one same-owner email Inbox source; three permitted contact bases; a 20/day hard maximum; at most two follow-ups; open/link tracking disabled; board-only direct sends/revocation; and accountable-owner proposals gated as `outbound_message` plus `customer_data_access`. Ten focused tests pass across shared validators, provider requests, embedded-database authorization/idempotency/suppression/reply behavior, and UI; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. A fresh isolated source runtime applied all 157 migrations through `0158`, proved one exact `system` / `provider.token` binding, rendered one active campaign, two evidence-backed leads, two messages, two global suppressions, seven receipts, an Inbox-routed reply, and zero horizontal overflow at 1440px and 390px across the Outreach page, Mission Control glance, and send dialog. Rollback closed ports 3124 and 54336; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. No live Postmark call was made. Live sender/domain acceptance, authenticated webhooks, signed public opt-out, address verification, analytics, stale-submission reconciliation, and a legal/provider-compliant true unsolicited cold-email rail remain unclaimed. | `doc/COMPANY-OUTREACH.md`, `server/src/services/company-outreach.ts`, `server/src/services/postmark-company-outreach-provider.ts`, `server/src/__tests__/company-outreach.test.ts`, `ui/src/pages/CompanyOutreach.tsx`, `work/company-outreach-canary-20260826/company-outreach-desktop.png`, `work/company-outreach-canary-20260826/company-outreach-mobile.png`, `work/company-outreach-canary-20260826/mission-control-outreach.png` |
| 2026-08-26 | Governed Company Ads core implemented and source-canaried | Summon had no company-level rail joining ad-account custody, public creative, audience policy, Website destination, maximum liability, provider lifecycle, performance evidence, approvals, and one board stop | Added 3 company-scoped tables and migration `0159`; 12 Ads API paths; one exact company-owned Meta account/Page and company-secret binding; one accountable Marketing owner; healthy Company Website enforcement; traffic-only adult/broad-country targeting; special-category and customer-list exclusion; $10–$1,000/day for 1–30 days; provider lifetime maximum; paused-first creation and ordered activation; board-only direct launch/pause/resume/revoke; and accountable-owner proposals gated as `budget_increase`, `public_claim`, and `outbound_message`. Ten focused tests pass across shared validators, provider requests, embedded-database authorization/idempotency/ambiguity/protective-pause behavior, and UI; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains at 102 unrelated pre-existing violations with zero in this slice. A fresh isolated runtime applied all 158 migrations through `0159`, proved one exact `system` / `provider.token` binding, rendered one active and one paused campaign with $63.50 spend, 18,700 impressions, 263 clicks, 202 landing views, six receipts, a Mission Control glance, and a fully reachable launch dialog. Desktop/mobile captures measured zero horizontal overflow at 1440px and 390px. No live Meta call was made. Live account/Page acceptance, credential onboarding, media upload/generation, Pixel/CAPI attribution, experiments, automated optimization, webhooks, non-USD budgets, stale-reservation recovery, and special-category paths remain unclaimed. | `doc/COMPANY-ADS.md`, `server/src/services/company-ads.ts`, `server/src/services/meta-company-ads-provider.ts`, `server/src/__tests__/company-ads.test.ts`, `ui/src/pages/CompanyAds.tsx`, `work/company-ads-canary-20260826/company-ads-desktop.png`, `work/company-ads-canary-20260826/company-ads-mobile.png`, `work/company-ads-canary-20260826/mission-control-ads.png` |

| 2026-08-26 | Portable Company Stack core implemented and source-canaried | Polsia could provision GitHub, Neon, and hosting while Summon only operated existing repositories, workspaces, and provider projects; there was no single owner-custodied path, partial-failure model, or board surface | Added 2 company-scoped tables and migration `0160`; 4 Stack API paths; one accountable Engineering owner; three distinct exact provider-secret bindings; fixed-origin GitHub, Neon, and Vercel adapters; a private auto-initialized repository; immediate Neon ID and encrypted URI capture; a GitHub-linked Vercel project; sensitive production/preview `DATABASE_URL`; stepwise receipts; non-replayed `partial`/`outcome_unknown` states; idempotent API retry behavior; and revocation that removes Summon authority without deleting owner resources. Nine focused tests pass across validation, provider requests/redaction/ambiguity, embedded-database authorization/idempotency/revocation, Stack UI, and Mission Control; shared/DB/server/UI typechecks, migration numbering/safety, and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in this slice. A fresh isolated runtime applied `0160`, proved four exact `system` bindings and seven receipts, rendered three of three resources while explicitly marking the application unclaimed, and measured zero horizontal overflow at 1440px and 390px across the Stack page, Mission Control glance, and fully reachable provisioning dialog. Rollback closed ports 3128 and 54340. No GitHub, Neon, or Vercel call was made. Live acceptance, OAuth, a code template/push, deployment/health, reconciliation/attach, database dump/restore, domain/DNS, arbitrary topologies, and mobile builds remain unclaimed. | `doc/COMPANY-STACK.md`, `server/src/services/company-stack.ts`, `server/src/services/portable-company-stack-provider.ts`, `server/src/__tests__/company-stack.test.ts`, `ui/src/pages/CompanyStack.tsx`, `work/company-stack-canary-20260826/company-stack-desktop.png`, `work/company-stack-canary-20260826/company-stack-mobile.png`, `work/company-stack-canary-20260826/mission-control-stack.png` |
| 2026-08-26 | Owner-published Public Company core and hosted address implemented and source-canaried | Polsia terms described public dashboards with logs, metrics, social posts, operational data, and company subdomains, while Summon had only private evidence surfaces and no explicit public/privacy contract | Added 2 company-scoped tables and migration `0161`; 7 Public Company API paths; a globally unique slug; private-by-default identity; separate search-indexing consent; a frozen aggregate canonical eight-department snapshot; allowlisted receipt-backed updates; board-only publish/withdraw/unpublish; immediate anonymous 404 on revocation; and an anonymous projection with no internal identifiers or raw evidence fields. `PAPERCLIP_PUBLIC_COMPANY_BASE_URL` now maps exactly one valid slug label beneath a trusted origin; reserved names are excluded, nested/malformed/unpublished hosts return uncached `noindex` 404s before the board shell, and the browser selects the company only from a server-injected slug. Initial HTML contains escaped description, robots, Open Graph, Twitter, and trusted canonical metadata and remains `no-store`. Twenty focused tests pass across shared validation, embedded-database authorization/redaction/revocation, hostname/metadata safety, static/Vite shell behavior, the public UI, and Mission Control; server/UI typechecks and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in this slice. Fresh isolated Vite and production-static runtimes rendered the hosted root and all eight rooms at 1440px and 390px with zero horizontal overflow; raw HTML contained the expected slug, noindex, Open Graph, and canonical tags; an unpublished label and a nested label both returned 404; and the prior audit found zero leaks across 12 planted private values. The static canary also caught and fixed stale packaged-UI precedence in source runs. Rollback closed ports 3130 and 54342. DNS/TLS deployment, custom domains, platform-wide discovery, anonymous abuse controls, and a deployed public canary remain unclaimed. | `doc/PUBLIC-COMPANY.md`, `server/src/public-company-html.ts`, `server/src/services/company-public.ts`, `server/src/__tests__/public-company-html.test.ts`, `server/src/__tests__/company-public.test.ts`, `ui/src/pages/CompanyPublic.tsx`, `ui/src/pages/PublicCompany.tsx`, `work/public-company-canary-20260826/public-company-hosted-desktop.png`, `work/public-company-canary-20260826/public-company-hosted-mobile.png`, `work/public-company-canary-20260826/public-company-board-desktop.png` |
| 2026-08-26 | Opt-in Public Company discovery and abuse controls implemented and source-canaried | Public Company could be shared directly but had no separately consented directory, anonymous traffic bounds, report intake, or durable board moderation loop | Added migration `0162`, a fifth independent discovery opt-in, opaque stable pagination, the uncached `noindex` Living companies directory, a company-scoped report queue, board-only durable resolutions, and sanitized activity receipts. Anonymous public HTML/JSON reads are limited to 120/minute per hashed client key with a 10,000-key memory cap; reports are separately limited to 5/hour plus 100 open/profile, ask for no contact identity, and return only `{accepted:true}`. Twenty-two focused tests pass across shared validation, migration safety, embedded-database opt-in/redaction/pagination/auth/audit/throttling, HTML metadata/rate limiting, and three UI surfaces; shared/DB/server/UI typechecks and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in this slice. The isolated runtime applied `0162`, returned only 12 allowlisted discovery keys with zero internal/report leaks, served discovery HTML and JSON as uncached `noindex`, accepted an anonymous report without an ID, recorded an actioned board resolution, and rendered discovery, report, and board moderation at 1440px and 390px with zero horizontal overflow. No public deployment, wildcard DNS/TLS, custom domain, or distributed edge limiter is claimed. | `doc/PUBLIC-COMPANY.md`, `packages/db/src/migrations/0162_company_public_discovery_abuse.sql`, `server/src/services/public-company-rate-limit.ts`, `server/src/services/company-public.ts`, `ui/src/pages/PublicCompanies.tsx`, `ui/src/pages/PublicCompany.tsx`, `ui/src/pages/CompanyPublic.tsx`, `work/public-company-canary-20260826/public-company-discovery-desktop.png`, `work/public-company-canary-20260826/public-company-discovery-mobile.png`, `work/public-company-canary-20260826/public-company-report-mobile.png`, `work/public-company-canary-20260826/public-company-reports-board-desktop.png` |
| 2026-08-26 | Governed Vercel custom-domain execution implemented and source-canaried | Website retained externally reported custom-domain state but could not attach, inspect, or verify the declared domain through its bound provider authority | Added `domains.execute` and a tenth Website API path. One explicit board action resolves the exact Website secret binding, checks the declared domain in the existing Vercel project, adds it when absent, attempts ownership verification, refreshes attachment state, and checks current DNS configuration. Only allowlisted access state, verification records, provider-recommended DNS values, and a receipt persist; agents are denied, failures are sanitized, and domain purchase, DNS writes, deletion, and raw provider responses are absent. Ten focused tests pass across shared validation, fixed-origin/provider sequencing/redaction, embedded-database authorization/secret receipts/success/failure, and UI; shared/server/UI typechecks pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in this slice. A migration-current isolated runtime through `0162` bound the scope, projected provider-shaped pending TXT/CNAME evidence, measured the production site at HTTP 200 in 480 ms, and rendered the board action and evidence with zero horizontal overflow at 1440px and 390px. Rollback closed ports 3130/54342; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced. The first older Website fixture exposed its already-known migration-lineage mismatch and was preserved. No live Vercel request was made without an explicitly authorized production credential. | `doc/COMPANY-WEBSITE.md`, `server/src/services/vercel-domain-provider.ts`, `server/src/services/company-website.ts`, `server/src/__tests__/company-website.test.ts`, `ui/src/pages/CompanyWebsite.tsx`, `work/vercel-domain-canary-20260826/vercel-domain-desktop.png`, `work/vercel-domain-canary-20260826/vercel-domain-mobile.png` |
| 2026-08-26 | Polsia-style idea → exact source → governed production → health chain implemented and source-canaried | Company Stack stopped honestly at wired infrastructure; an empty repository could not become a visible application or join the existing deployment/health evidence chain | Added migration `0164`, one immutable initial application plan per Stack, the ten-file `summon-next-company` v1 template (Next.js 16.3.2, React 19.2.8, encrypted PostgreSQL contract, agent rules, `/api/health`), exact Git tree/commit/non-force ref publication, private repository identity checks, immediate ref reconciliation, definite safe retry, unknown-outcome replay blocking, automatic governed Website connection, exact-SHA Vercel deployment input, validated health-path probing, and a four-stage board UI that refuses to collapse Idea, Source, Production, and Health. Focused coverage parses every generated module and proves validators, stable manifest identity, Git/Vercel request bodies, redaction, ambiguity, authorization, idempotency, immutable-plan conflict, secret bindings, approval bridge, health-path safety, and desktop/mobile projection. Shared/DB/server/UI typechecks, migration safety, and production UI build pass. The token gate remains exactly 102 unrelated pre-existing violations with zero in the slice. A fresh isolated runtime through `0164` proved five exact bindings, ten manifest files, exact source/deploy SHA, healthy `/api/health` evidence, and zero horizontal overflow at 1440px/390px; the canary caught and fixed a desktop commit wrapping defect. Rollback closed ports 3131/54329; the known Ctrl-C drain `ECONNRESET` reproduced. Zero live GitHub, Neon, or Vercel calls were made, so live provider acceptance and one live approved source-to-health receipt remain open gates. | `doc/COMPANY-STACK.md`, `doc/COMPANY-WEBSITE.md`, `packages/db/src/migrations/0164_company_stack_application.sql`, `server/src/services/summon-application-template.ts`, `server/src/services/github-application-source-provider.ts`, `server/src/services/company-stack.ts`, `ui/src/pages/CompanyStack.tsx`, `work/stack-application-canary-20260826/stack-application-desktop.png`, `work/stack-application-canary-20260826/stack-application-mobile.png` |

| 2026-08-26 | Durable exact-channel board notifications implemented and migration-tested | “Runs while you sleep” still required the board to remember to reopen Mission Control; daily briefs, Nightshift failures, hard stops, decisions, customer arrivals, and high-severity alerts had no durable outbound delivery contract | Migration `0166` adds a company-scoped notification outbox and append-only receipts. A separately granted Slack `notifications.send` capability admits exactly seven authoritative activity classes, projects only fixed safe copy, deduplicates connector plus activity, reserves before provider submission, uses the delivery UUID as `client_msg_id`, attempts one due item per connector per existing scheduler tick, retries only definite failures on a bounded five-attempt schedule, blocks replay after ambiguous or stale outcomes, and revokes queued work when exact authority disappears. Customers exposes delivery receipts and definite retry; Mission Control reports pending and attention counts. Twenty-five focused tests across six files pass, including migration-current capture, redaction, exact target, dedupe, retry, ambiguity, revocation, Inbox regression coverage, projection, and receipts. Shared/DB/server/UI typechecks, migration numbering/safety, and the UI production build pass. The token gate remains at exactly 102 unrelated pre-existing violations with none in changed files. The default runtime was not started and no live Slack call was made. | `doc/COMPANY-NOTIFICATIONS.md`, `packages/db/src/migrations/0166_company_notifications.sql`, `server/src/services/company-notifications.ts`, `server/src/__tests__/company-notifications.test.ts`, `ui/src/pages/CustomerInbox.tsx`, `ui/src/components/MissionControl.tsx` |

| 2026-08-26 | Board-only Company Stack database snapshots implemented and migration-tested | Company Export already carried Summon's portable control-plane configuration, but the customer application's PostgreSQL data had no packaged exit artifact even though Polsia advertises database export | Migration `0167` adds one company/Stack-scoped snapshot record and five append-only Stack receipt types. One explicit board action resolves only the exact `system` / `database.url` binding, starts `pg_dump` without a shell, keeps the password only in `PGPASSWORD`, separates host/port/user/database arguments, requires TLS, rejects arbitrary URL options, emits custom format without owner or privileges, enforces a 128 MiB and 10-minute cap, validates `PGDMP`, retains at most three ready objects through company-scoped StorageService, and persists only size, SHA-256, normalized tool version, expiry, and safe failure codes. Downloads are private/no-store; board deletion and 24-hour scheduler/list expiry physically delete the object and clear its object key; restore mutation remains unapproved. Nine focused server tests and three Stack UI tests pass, covering process isolation, bounds, authorization, attestation, concurrent dedupe, retention cap, redaction, storage, download, delete, failure, expiry, and checksum presentation. Shared/DB/server/UI typechecks and migration numbering/safety pass. No default runtime, live provider, or live database was touched. | `doc/COMPANY-STACK.md`, `packages/db/src/migrations/0167_company_stack_database_snapshots.sql`, `server/src/services/postgres-database-snapshot-provider.ts`, `server/src/services/company-stack-database-snapshots.ts`, `server/src/__tests__/company-stack-database-snapshots.test.ts`, `ui/src/pages/CompanyStack.tsx` |

| 2026-08-26 | Authenticated Stripe checkout-to-fulfillment rail implemented and migration-tested | Company Payments could publish and poll Stripe Payment Links, but a completed checkout did not durably create delivery work or wake the company | Migration `0168` adds an opaque endpoint identity, separate exact `system / webhook.signing_secret` binding, webhook readiness/evidence, session/type receipt idempotency, and one company/session `customer_payment` task invariant. The public route sits outside board auth but behind request-size, host, and 120/minute endpoint/client controls; it verifies Stripe's raw-body `t`/`v1` HMAC within five minutes before parsing, requires test/live agreement, admits only three Checkout Session events, discards raw bodies and customer/metadata fields, and maps only Payment Links created by the same Summon account. A paid session creates one high-priority task and wakes the accountable owner; duplicate delivery reuses it; pending, failed, and unrelated signed checkouts become bounded receipts without false fulfillment. Nine focused tests across four files pass, including signature/tamper/expiry/event projection, PII omission, board-only setup, exact secret access, duplicate delivery, one task/wake, and unmapped isolation. Shared/DB/server/UI typechecks, migration numbering/safety, and the production UI build pass. The token gate remains exactly 102 unrelated pre-existing violations with none in the Payments slice. The embedded-Postgres Windows cleanup helper now retries bounded handle release after stop. No live Stripe request, test charge, deployment, or source-runtime start was made. | `doc/COMPANY-PAYMENTS.md`, `packages/db/src/migrations/0168_company_payment_webhooks.sql`, `server/src/services/stripe-company-payment-webhook.ts`, `server/src/services/company-payment-webhooks.ts`, `server/src/routes/company-payment-webhooks.ts`, `server/src/__tests__/company-payments.test.ts`, `ui/src/pages/CompanyPayments.tsx` |
| 2026-08-26 | Governed Stripe customer/refund/dispute operations implemented and migration-tested | Company Payments could accept and fulfill a checkout but had no customer-protection authority, refund workflow, or dispute visibility | Migration `0169` adds a separate exact `system / operations.credential` binding, readiness and latest-100 aggregate customer/refund/dispute metrics, one immutable refund request model, and redacted dispute projections. Configuration proves the same Stripe account/mode and read permissions before persisting. A board refund must bind to an authenticated mapped Checkout receipt and live-matching Payment Link, PaymentIntent, amount, and currency; only duplicate/customer-request reasons are admitted. The request is reserved before the provider call, uses a stable Stripe idempotency key plus Summon metadata, and never replays an ambiguous outcome; reconciliation reads by PaymentIntent and metadata. Customer identity, refund destinations, payment methods, metadata, and dispute evidence are discarded. Ten focused validator/provider/UI tests and three embedded-database Payments tests pass; the latter prove exact secret access, board-only authority, one provider call across retries, redacted disputes, ambiguous outcome persistence, no replay, and explicit reconciliation. Shared/DB/server/UI typechecks, migration numbering/safety, and the UI production build pass at this checkpoint. The repository token gate remains at its pre-existing 102-item baseline, with no violation in the Payments surface. No live Stripe request, refund, deployment, default-runtime start, or provider credential use occurred. | `doc/COMPANY-PAYMENTS.md`, `packages/db/src/migrations/0169_company_payment_operations.sql`, `server/src/services/company-payment-operations.ts`, `server/src/services/stripe-company-payments-provider.ts`, `server/src/__tests__/company-payments.test.ts`, `ui/src/pages/CompanyPayments.tsx` |
| 2026-08-26 | Governed exact-revision Expo/EAS mobile builds implemented and migration-tested | The parity map still marked mobile builds as a primitive: Summon could neither bind a company-owned Expo project nor connect one board-authorized source revision to inspectable cloud-build evidence | Migration `0170` adds one active mobile app per company, one unresolved build per app, request and provider-run uniqueness, and append-only build receipts. Company Stack now binds one exact `system / expo.workflow_token` secret, dispatches only the repository's declared workflow at a 40-character commit SHA, reserves before the provider mutation, projects bounded run/build/submission IDs and statuses, verifies the provider commit, blocks duplicate and ambiguous replay, permits board-only read reconciliation for unknown or stale dispatches, and revokes Summon authority without deleting owner resources. Eleven focused tests across shared validation, fixed-origin Expo projection/failure classification, migration-current embedded-database authorization/idempotency/reconciliation, the Mobile UI, and Stack integration pass. Shared/DB/server/UI typechecks, migration numbering/safety, and the UI production build pass. The token gate remains exactly 102 unrelated pre-existing violations with none in the Mobile surface. No Expo credential, provider request, build capacity, signing key, app-store account, default runtime, or production deployment was touched. Live disposable-build acceptance, mobile source generation/project linking, artifact/install receipts, store submission, updates, webhooks, and provider-spend evidence remain unclaimed. | `doc/COMPANY-MOBILE.md`, `packages/db/src/migrations/0170_company_mobile_builds.sql`, `server/src/services/expo-company-mobile-provider.ts`, `server/src/services/company-mobile-builds.ts`, `server/src/__tests__/company-mobile-builds.test.ts`, `ui/src/components/CompanyMobileBuilds.tsx`, `ui/src/pages/CompanyStack.tsx` |
| 2026-08-26 | Deterministic mobile idea → exact Expo source → governed EAS build handoff implemented and source-canaried | The governed build rail still required a separately created mobile codebase and manually copied commit, so Summon could not turn the board's fixed mobile idea into auditable owner-custodied source | Migration `0171` adds one immutable source plan per Mobile app, exact private-repository/EAS-project/store identifiers, template/manifest identity, pre-mutation status, source receipts, and separate exact `system / github.token` custody. One board action now generates the 12-file `summon-expo-company` v1 starter (Expo SDK 57, Expo Router, React Native 0.86.2, strict TypeScript, dev client, EAS profiles/workflow, frozen Bun 1.3.14 lock), publishes one parented non-force commit through the existing fixed-origin GitHub adapter, blocks replay after ambiguous publication, and supplies the resulting SHA to the build dialog. Sixteen focused tests pass: seven validator/template/provider, four migration-current embedded-database custody/idempotency/ambiguity/revocation, and five Mobile/Stack UI tests. Shared/DB/UI typechecks, migration numbering/safety, and the production UI build pass; the token gate remains exactly 102 unrelated pre-existing violations with none in Mobile. The full server typecheck is presently stopped only by two unrelated `server/src/services/recovery/service.ts` field-narrowing diagnostics and emits no Mobile error. An isolated generated-app canary passed frozen dependency install, strict TypeScript, Expo web export, and Expo Android export with manifest hash `0b4c83ffc5def81d562e26ddfc3d3596232f74b86f38cb145fd359e8410b3dd8`; its receipt explicitly says no cloud build and no physical-device test. No live GitHub/Expo credential, provider mutation, default runtime, signing key, or store account was touched. Managed repository/EAS provisioning/linking, unresolved-source reconciliation, icons/assets, install/physical-device receipts, store submission, updates, webhooks, and spend evidence remain open. | `doc/COMPANY-MOBILE.md`, `packages/db/src/migrations/0171_company_mobile_source.sql`, `server/src/services/summon-mobile-application-template.ts`, `server/src/services/company-mobile-builds.ts`, `server/src/__tests__/company-mobile-builds.test.ts`, `ui/src/components/CompanyMobileBuilds.tsx`, `scripts/verify-summon-mobile-template.ts` |

| 2026-08-26 | Ambiguous Mobile source publication made safely recoverable without replay | The new Expo source rail blocked duplicate mutation correctly, but an unknown or crash-abandoned GitHub outcome could only tell the board to inspect the repository; it could not attach verified source or unblock the governed build chain | Added one board-only `source/reconcile` action with an exact 40-character candidate SHA and the already-bound GitHub credential. The provider performs four GET-only proofs: exact private repository, exact commit with immutable source/manifest trailers and one parent, candidate reachability from the default branch, and byte-for-byte Git blob SHA equality for every one of the 12 planned files. Only complete proof moves source to `source_ready`; missing, divergent, altered, truncated, or network-inconclusive evidence remains `outcome_unknown`, records a sanitized receipt, permits another read-only inspection, never calls the publisher, and blocks cloud build. Nineteen focused tests pass: nine validator/template/provider, four migration-current embedded-database authority/custody/idempotency/reconciliation, and six Mobile/Stack UI tests. Shared/DB/UI typechecks, migration numbering/safety, and the production UI build pass; token gates remain at the exact unrelated 52 + 32 + 18 baseline with no Mobile finding. The full server typecheck emits no Mobile diagnostic and remains stopped only by two unrelated recovery-service field-narrowing errors. No provider credential, GitHub mutation, Expo request, default runtime, or production deployment was used. | `server/src/services/github-application-source-provider.ts`, `server/src/services/company-mobile-builds.ts`, `server/src/routes/company-mobile-builds.ts`, `server/src/__tests__/company-mobile-builds.test.ts`, `ui/src/components/CompanyMobileBuilds.tsx`, `doc/COMPANY-MOBILE.md` |
| 2026-08-26 | Signed Expo build/artifact receipts implemented and migration-tested | The Mobile chain could verify an EAS workflow but stopped before an installable binary: artifact links required manual Expo inspection, and Summon had no authenticated event boundary | Migration `0173` adds one unguessable Build-webhook endpoint, separate exact signing-secret custody, webhook health timestamps, two receipt types, and provider-build deduplication. The parser verifies Expo's documented HMAC-SHA1 signature over the exact raw bytes with constant-time comparison, then admits only the configured project and a build at the authorized commit/profile/platform. It projects the build UUID, safe `expo.dev` details/artifact URLs, file kind, distribution, conservative installability, and completion time; raw payloads, logs, identities, messages, metrics, errors, credentials, and binaries are discarded. Out-of-band same-project builds attach nothing; redelivery creates one receipt; provider events never infer whole-workflow success. The Stack UI exposes one signed-receipt setup and Android/iOS/simulator/store artifact links. Twenty-three focused tests pass: eleven validator/template/provider/parser, five migration-current embedded-database custody/authorization/idempotency/redaction, and seven Mobile/Stack UI tests. Shared/DB/UI typechecks, migration checks, and the production UI build pass. The token gate remains at its exact unrelated 52 + 32 + 18 baseline with no Mobile finding. The full server typecheck emits no Mobile diagnostic and remains stopped only by the same two unrelated recovery-service field-narrowing errors. No live Expo credential, build, webhook, artifact, default runtime, or physical device was used. | `packages/db/src/migrations/0173_company_mobile_webhooks.sql`, `server/src/services/expo-company-mobile-webhook.ts`, `server/src/services/company-mobile-webhooks.ts`, `server/src/routes/company-mobile-webhooks.ts`, `server/src/__tests__/company-mobile-builds.test.ts`, `ui/src/components/CompanyMobileBuilds.tsx`, `doc/COMPANY-MOBILE.md` |
| 2026-08-26 | Governed exact-artifact mobile store submissions implemented and migration-tested | Signed production `.aab`/`.ipa` evidence still ended at a link; Summon could not bind the exact binary to a bounded Google Play or App Store Connect submission receipt | Migration `0174` adds versioned v2 source release identity, one immutable platform submission per exact build, unresolved-run exclusion, provider-run uniqueness, and append-only release receipts. The v2 template adds a separate `submit` workflow and platform-specific production profile while reproducing v1 byte-for-byte. Android stops at Google Play internal `Draft`; iOS stops at App Store Connect/TestFlight. The board cannot inject a provider build ID: Summon derives it from signed production artifact evidence, reserves before dispatch, pins the verified source/build commit, and blocks ambiguous replay. Provider success requires that exact commit, build UUID, and a submission UUID; missing evidence stays `action_required`, divergence becomes `revision_mismatch`, and every projection says public release is not claimed. Twenty-five focused tests pass: eleven shared/template/provider/webhook tests, six migration-current embedded Mobile tests, and eight Mobile/Stack UI tests. Shared/DB/UI typechecks, migration checks, and the production UI build pass. The token gate remains at the exact unrelated 52 + 32 + 18 baseline with no Mobile finding. Server typecheck emits no Mobile diagnostic and remains stopped only by the same two unrelated recovery-service errors. No live Expo, Google Play, App Store Connect, credential, build, upload, default runtime, or deployment was used. | `packages/db/src/migrations/0174_company_mobile_releases.sql`, `server/src/services/company-mobile-builds.ts`, `server/src/services/expo-company-mobile-provider.ts`, `server/src/routes/company-mobile-builds.ts`, `server/src/__tests__/company-mobile-builds.test.ts`, `ui/src/components/CompanyMobileBuilds.tsx`, `doc/COMPANY-MOBILE.md` |
| 2026-08-26 | Fixed-origin Company AI Gateway implemented and migration-tested | Polsia's terms explicitly offered API endpoints including OpenAI-compatible interfaces, while Summon had only internal/agent APIs and no application-facing AI authority envelope | Migration `0175` adds one company gateway, hashed one-time application keys, and metadata-only request receipts. The board binds an active company-owned OpenAI project secret, accountable owner, dated model snapshot, strict text-only contract, per-request/rate/concurrency/monthly token limits, and immediate key/gateway revocation. The public endpoint sits outside board auth but authenticates only gateway keys; it locks the gateway before admission, honors existing budget hard stops, reserves a conservative UTF-8 token envelope before dispatch, calls only OpenAI's first-party Chat Completions origin, and forces `stream: false`, `store: false`, one choice, and the pinned model. Duplicate request UUIDs and ambiguous outcomes never replay; two-minute abandoned reservations become unreplayed `outcome_unknown` receipts. Prompt, answer, customer identity, raw provider response/error, provider credential, plaintext application key, and failed-request HTTP body are excluded from persistence/logging. Fourteen focused validator/provider/migration-current/gateway-UI/Stack tests pass; shared/DB/UI typechecks, migration numbering/safety, gateway-scoped whitespace checks, and the production UI build pass. The token gate remains at the exact unrelated 52 + 32 + 18 baseline with no gateway finding. Server typecheck emits no AI-gateway diagnostic and remains stopped only by the two unrelated recovery-service narrowing errors. The repository-wide diff check still reports unrelated pre-existing trailing whitespace in handoff files. No live OpenAI request, credential, provider billing, default runtime, or deployment was used. | `doc/COMPANY-AI-GATEWAY.md`, `packages/db/src/migrations/0175_company_ai_gateway.sql`, `server/src/services/company-ai-gateway.ts`, `server/src/services/openai-company-ai-gateway-provider.ts`, `server/src/routes/company-ai-gateway-inference.ts`, `server/src/__tests__/company-ai-gateway.test.ts`, `ui/src/components/CompanyAiGateway.tsx` |

| 2026-08-28 | Provider-neutral financial statement reconciliation implemented and rendered | Profitability and runway could combine live Stripe evidence with AI spend and board-entered expenses, but there was no repeatable statement-backed path or way to distinguish manual and imported expense provenance | Added a board-only, company-scoped bank/accounting CSV import with a strict 2 MB/1,000-row USD contract, all-or-nothing validation, hashed account and statement provenance, transaction-ID or occurrence-aware composite fingerprints, overlapping-export deduplication, and a receipt that exposes evidence strength. Mission Control and the Aether company projection now distinguish AI-only, board-recorded, statement-backed, and mixed expense evidence. Forty focused tests pass, including embedded-Postgres import/re-import, identical-purchase preservation, raw-input non-persistence, and atomic currency rejection. Shared/UI typechecks and the production UI build pass; server typecheck emits no finance diagnostic and remains stopped only by two pre-existing recovery narrowing errors. The token gate remains at the exact 52/32/18 baseline with no statement-import file listed. Helium renders the control and dialog at desktop and 390×844 with zero overflow. No real statement was submitted and no bank/accounting provider was contacted. | `doc/finance/STATEMENT-IMPORT.md`, `server/src/services/finance.ts`, `server/src/routes/costs.ts`, `ui/src/components/ImportFinanceStatementDialog.tsx`, `ui/src/components/MissionControl.tsx` |

| 2026-08-28 | Current Polsia Auto Mode duration parity implemented | Summon's evidence ledger and Nightshift still followed an older 1–12 hour help snapshot even though Polsia's deployed control had expanded to five days | Read-only inspection of Polsia's deployed public interface established the current set as 1–12, 18, 24, 48, 72, and 120 hours and separately confirmed that seven-day God Mode is a retired historical product. Summon's shared contract, request validator, direct service guard, and UI now expose exactly that set, render whole-day values as days, and reject unsupported gaps such as 13 or 121 hours. Thirty-two focused tests pass; shared/UI typechecks and the production build pass. Server typecheck emits no Company Loop diagnostic and remains stopped only by two pre-existing recovery narrowing errors. Token gates remain at the exact unrelated 52/32/18 baseline with no changed Company Loop file listed. No Nightshift, provider call, deployment, or default-runtime mutation occurred. | `packages/shared/src/types/company-loop.ts`, `packages/shared/src/validators/company-loop.ts`, `server/src/services/company-loop.ts`, `ui/src/components/CompanyLoop.tsx`, `doc/execution-semantics.md`, `FEATURE-PARITY.md` |

| 2026-08-28 | Polsia-style autonomy workspace implemented and rendered | Expanding Company controls stacked Decide, Nightshift, and Brief inside one narrow Mission Control column, leaving most of the authenticated-reference desktop workspace blank | The Routines row now opens one dismissible shadcn dialog while preserving the dense company workspace and Board chat behind it. Company Loop uses three desktop columns and one mobile column; transparent child surfaces preserve the dialog elevation contract. Seventeen focused Mission Control and Company Loop tests pass, UI typecheck passes, and the production build passes. Helium measured three approximately 385px columns in the 1,280px desktop dialog and one 267px content column in the 358px mobile dialog; both report zero root overflow and render `5 days`, `$5.00`, and `Start 5d Nightshift`. The token gate remains at the exact unrelated 52/32/18 baseline with no changed file listed. No Nightshift, external provider call, deployment, or main-runtime mutation occurred. | `ui/src/components/MissionControl.tsx`, `ui/src/components/CompanyLoop.tsx`, `ui/src/components/FleetRunningNow.tsx`, `ui/src/components/MissionControl.test.tsx`, `ui/src/components/CompanyLoop.test.tsx`, `DESIGN.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\Aether\helium-harness\summon-company-controls-wide-5-day-2026-08-28.png`, `C:\Users\adamp\Aether\helium-harness\summon-company-controls-mobile-nightshift-5-day-2026-08-28.png` |

| 2026-08-28 | Work-attached company-world prompt implemented and rendered | The gamified office showed real employees and work but selecting a villager could only inspect or open it; the existing Messages surface was a local-only prototype and therefore could not truthfully receive direction | Added one middle-bottom HUD over the map. It enables only for a staffed villager with non-terminal current work, focuses without scrolling, names the exact task, warns that Enter may wake the employee and use provider budget, and posts through the existing durable issue-comment contract. Optimistic movement uses the still-unstyled `you` placeholder and transform-only reduced-motion-safe motion; the complete instruction never appears in a bot bubble, while the player bubble clips to 22 characters. An explicit no-reopen server boundary prevents the post from reviving work that completes during the 650ms walk while leaving default comment semantics unchanged. Shipped/cancelled work stays closed, delivery failure remains visible, and the fixed card plus HUD do not shift the map. Four focused UI tests and one focused server race-boundary test, UI typecheck, and the production build pass. Helium measured zero root/prompt/card overflow at 2,134×1,059 and 390×844. A desktop draft was typed and discarded by navigation; no comment, wake, provider call, spend, deployment, or main-runtime mutation occurred. Token gates remain at the inherited 52/32/18 baseline; Company World adds no component finding and the reported Factory Floor findings predate this slice. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/pages/FactoryFloor.tsx`, `ui/src/index.css`, `server/src/routes/issues.ts`, `server/src/__tests__/issue-comment-reopen-routes.test.ts`, `DESIGN.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\Aether\helium-harness\summon-company-world-prompt-desktop-2026-08-28.png`, `C:\Users\adamp\Aether\helium-harness\summon-company-world-prompt-mobile-2026-08-28.png` |

| 2026-08-28 | Provider-neutral Company Media implemented and rendered | Polsia's authenticated dashboard exposed Image and Video actions, while Summon's parity ledger mentioned media only inside generic Artifacts and had no request path, accountable owner, budget warning, or blocked-state contract | Added `/company/media`: Image and Video create ordinary project-aware tasks with stable `summon.company.media.*` billing identity, one employee owner, an explicit assignment-wake/provider-budget warning, and an artifact-based definition of done. The task forbids publishing, buying credits, adding provider authority, or copying credentials; an unavailable capability must return `BLOCKED`. The page and Mission Control show only real image/video artifacts and real task state. Four focused contract/page tests, UI typecheck, and the production build pass. The token gate remains at the exact unrelated 52/32/18 baseline with no Company Media finding. Helium measured zero root/page/dialog overflow at desktop and 390×844, with no alerts; the empty brief keeps `Assign image` disabled. One stale HMR load reached the existing blank-screen guard immediately after the build and recovered on a clean reload. No media request, task, wake, provider call, credential use, spend, publication, deployment, runtime restart, or 3100 mutation occurred. | `ui/src/lib/company-media.ts`, `ui/src/pages/CompanyMedia.tsx`, `ui/src/components/MissionControl.tsx`, `ui/src/App.tsx`, `DESIGN.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\Aether\helium-harness\summon-company-media-desktop-2026-08-28.png`, `C:\Users\adamp\Aether\helium-harness\summon-company-media-mobile-dialog-2026-08-28.png`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-company-media-2026-08-28` |
| 2026-08-28 | Vercel production versions and complete governed recovery loop implemented | Polsia exposed Website Versions, Redeploy, rollback, and Promote while Summon retained only the latest deployment receipt and could not inspect, rebuild, restore, or unpin an immutable production build | Migration `0176` adds redeploy, rollback, and promotion receipts plus unique request and shared in-flight reservations. Website now reads Vercel's ready production builds from the fixed REST origin, discards unsafe URLs and provider/account payloads, projects exact deployment/source identity plus Vercel's `isRollbackCandidate`, and gives a still-present recorded deployment precedence when identifying Current after rollback. Redeploy Current rebuilds the exact current provider-confirmed deployment with inherited settings; Instant Rollback reroutes only a currently eligible prior build; Promote sends an exact ready non-current, non-rollback-candidate build to production without rebuilding and records restored automatic domain assignment. All three are board-only, scope-limited, reasoned, UUID-correlated, typed-confirmation actions; they block parallel production mutations, return the original receipt on replay, and preserve last-known production after provider rejection. The dialogs distinguish provider build/budget from no-rebuild traffic mutations and explain environment, cron, external-system, and automatic-domain-assignment boundaries. Eleven focused provider/server/UI tests pass; shared, DB, and UI typechecks plus migration numbering/safety pass. Server typecheck emits no Website diagnostic and remains stopped only by the same two unrelated recovery timestamp errors. The isolated 3103 runtime stayed read-only; no Vercel credential, provider request, rollback, redeploy, promotion, deployment, runtime restart, or 3100 mutation occurred. | `packages/db/src/migrations/0176_company_website_rollbacks.sql`, `server/src/services/vercel-deployment-provider.ts`, `server/src/services/company-website.ts`, `server/src/__tests__/company-website.test.ts`, `ui/src/pages/CompanyWebsite.tsx`, `doc/COMPANY-WEBSITE.md`, `FEATURE-PARITY.md` |
| 2026-08-28 | Visual office interaction and immersive viewport contract completed | The supplied botvillage contract required first-click sound and an exactly screen-sized fixed simulation, but Company World began muted and Helium measured the map at 1,136px inside a 1,011px scroll region with Paperclip chrome around it | Audio remains gesture-bound and now starts on the first villager click; `M` remains the explicit mute override and no autoplay context is created beforehand. Factory Floor is now a dedicated immersive route: no sidebar, breadcrumbs, banners, properties, feedback widget, mobile navigation, or page scroll; the real ranked queue remains available from one HUD action. The island flexes into the remaining HUD/footer viewport while preserving nine real control-plane positions, day/night preview, the unchosen-player boundary, prompt gating, and durable task direction. Nineteen focused Company World and Layout tests, UI typecheck, and the production build pass. The exact inherited 52/32/18 token baseline remains with no new violation. Recorded Helium desktop and 390×844 acceptance show client dimensions equal scroll dimensions and no app chrome. No prompt, comment, wake, provider action, spend, deployment, or main-runtime mutation occurred. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/components/Layout.tsx`, `ui/src/components/Layout.test.tsx`, `ui/src/pages/FactoryFloor.tsx`, `ui/src/index.css`, `DESIGN.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-world-immersive-acceptance`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-world-mobile-final`, `C:\Users\adamp\Aether\helium-harness\summon-world-immersive-desktop-final-20260828.png`, `C:\Users\adamp\Aether\helium-harness\summon-world-immersive-mobile-final-20260828.png` |
| 2026-08-28 | Audited company-chat file attachments implemented and rendered | Polsia's current persistent chat exposes working paperclip and microphone controls, while Summon's Board Chat used the same shared composer without attachments and the parity ledger incorrectly dismissed both controls as ornamental | Board Chat now prepares its standing `Board Operations` issue only when the board explicitly chooses the first file, uploads up to eight company-owned issue attachments, exposes staged/uploaded/error state plus removal, and carries only attached UUIDs into the send. The server validates the requested task and every attachment against the same company and issue, rejects invalid/duplicate references, escapes untrusted metadata, and supplies fixed local content paths without persisting raw file content in the chat turn. Twenty-three focused UI and nine server tests pass; UI typecheck and the production build pass. Server typecheck emits no Board Chat error and remains stopped only by the two unrelated recovery timestamp errors. The token gate remains at 52/32/18 with no changed-file finding. Helium renders the enabled paperclip at `/SUM/chat` with zero horizontal overflow. No picker, upload, message, wake, provider, spend, deployment, or runtime mutation occurred. Voice transcription remains explicitly partial rather than inferred. | `ui/src/api/board-chat.ts`, `ui/src/components/ChatComposer.tsx`, `ui/src/components/ChatComposer.test.tsx`, `ui/src/pages/BoardChat.tsx`, `ui/src/pages/BoardChat.test.tsx`, `server/src/routes/board-chat.ts`, `server/src/routes/openapi.ts`, `server/src/__tests__/board-chat-route-feature-flag.test.ts`, `FEATURE-PARITY.md`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-board-chat-attachments-final-2`, `C:\Users\adamp\Aether\helium-harness\summon-board-chat-attachments-final-20260828.png` |
| 2026-08-29 | Governed company-chat voice direction implemented and source-canaried | Polsia's audited microphone was a real transcription control while Summon had no provider, permission, cost, retention, failure, or review contract | Migration `0177` adds one metadata-only transcription ledger and a separately enabled Company AI Gateway policy. The board pins `gpt-4o-mini-transcribe-2025-12-15`, 15–120 seconds, current input/output rates, and a $0.025 request reservation inside a hard monthly micro-dollar ceiling. The browser asks for microphone permission only on click, auto-stops, and inserts the result as an editable draft without auto-send. Server admission is company-scoped, one-at-a-time, budget-checked, UUID-correlated, fixed-origin, exact-secret-bound, and non-replayable after ambiguity; it records only model/usage/cost/status, zeroes every audio buffer, and stores neither audio nor transcript. Voice estimates join the current-month AI expense total without losing micro-dollar evidence. Thirty-one focused server/shared and thirty-one focused UI tests pass; shared, database, and UI typechecks, migration numbering/safety, and the production UI build pass. Server typecheck emits no voice error and remains stopped only by the two unrelated recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no voice file listed. A fresh isolated database applied all migrations through `0177`; Helium rendered the enabled mic at desktop and 390×844 with no alert or horizontal overflow. No microphone capture, credential, OpenAI request, spend, message, deployment, or main-runtime mutation occurred, so the live-provider row remains blocked by evidence. | `packages/db/src/migrations/0177_company_chat_transcription.sql`, `server/src/services/company-ai-gateway.ts`, `server/src/services/openai-company-transcription-provider.ts`, `server/src/routes/board-chat.ts`, `ui/src/pages/BoardChat.tsx`, `ui/src/components/ChatComposer.tsx`, `ui/src/components/CompanyAiGateway.tsx`, `doc/COMPANY-AI-GATEWAY.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-board-chat-voice-20260829`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-board-chat-voice-mobile-20260829`, `C:\Users\adamp\Aether\summon.company\screenshots\summon-board-chat-voice-source-20260829.png` |
| 2026-08-29 | Recipient-controlled Company Outreach unsubscribe implemented and source-canaried | Outreach could suppress after a board action or normalized provider event, but the recipient-facing footer had no working public stop path | Migration `0178` adds one message-bound 256-bit opaque capability and stores only its SHA-256 hash. Every provider submission receives the plain-text link plus RFC 8058 one-click headers. GET is confirmation-only against mail scanners; POST is generic and idempotent, creates the existing company-global suppression exactly once, and cannot reveal identity, send, wake, spend, or reopen work. The route is separately rate-limited, `no-store`/`noindex`/no-referrer/CSP protected, and scrubs the bearer from application logs before database work. Five focused server/provider tests and two Outreach UI tests pass, including the legacy-sender reconnect state; database, shared, and UI typechecks plus migration checks and the production UI build pass. Server typecheck emits no Outreach diagnostic and remains stopped only by the two unrelated recovery timestamp errors. Token gates remain at the exact inherited 52/32/18 baseline with no Outreach file listed. A fresh isolated runtime applied every migration through `0178`, returned health/bootstrap ready, served confirmation and completion responses, and logged zero raw-token matches. Helium rendered the editorial black-and-white confirmation at desktop and 390×844 with zero overflow. No Postmark credential, provider request, email, deployment, spend, or main-runtime mutation occurred, so the feature remains blocked only by live provider evidence. | `packages/db/src/migrations/0178_company_outreach_unsubscribe.sql`, `server/src/services/company-outreach.ts`, `server/src/routes/company-outreach-unsubscribe.ts`, `server/src/services/postmark-company-outreach-provider.ts`, `ui/src/pages/CompanyOutreach.tsx`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-outreach-unsubscribe-20260829`, `C:\Users\adamp\.config\browser-harness\agent-workspace\recordings\summon-outreach-unsubscribe-mobile-20260829`, `C:\Users\adamp\Aether\summon.company\screenshots\summon-outreach-unsubscribe-mobile-20260829.jpg` |
| 2026-08-29 | Authenticated Postmark Outreach event lifecycle implemented and source-canaried | Sender setup could submit to Postmark while provider delivery, hard-bounce, and complaint evidence still depended on a manually normalized internal event | Migration `0179` adds provider webhook identity, hash-only authentication authority, readiness/error state, and event timestamps. Sender connection creates or reuses one exact stream/URL Postmark webhook, rotates a 256-bit Basic Auth password, explicitly verifies it, and blocks reservation until verified. The public route accepts only exact server/stream/message/recipient Delivery, hard Bounce, and SpamComplaint facts, no-ops provider verification samples and soft/unsupported events, deduplicates trace IDs or canonical hashes, and stores no raw payload/content/metadata. Revocation invalidates local authority even if provider deletion fails. Ten focused server/provider/database tests and two focused UI tests pass; shared, database, and UI typechecks and the production UI build pass. Server typecheck has no Outreach diagnostic and stops only at the two unrelated recovery timestamp errors. Token gates remain the exact inherited 52/32/18 baseline with no Outreach file listed. Isolated 3105 applied `0179`, returned healthy, preserved unsubscribe, rejected bad auth and malformed IDs with bounded public responses, redacted Authorization, and logged zero test body/auth markers. Ports 3103/3104 stayed up and 3100 stayed down. No credential, Postmark call, email, spend, deploy, or main-runtime mutation occurred. | `packages/db/src/migrations/0179_company_outreach_webhooks.sql`, `server/src/services/postmark-company-outreach-provider.ts`, `server/src/services/postmark-company-outreach-webhook.ts`, `server/src/services/company-outreach.ts`, `server/src/routes/company-outreach-webhooks.ts`, `server/src/__tests__/company-outreach.test.ts`, `ui/src/pages/CompanyOutreach.tsx`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Hunter-backed Outreach address verification implemented and source-canaried | Evidence-backed contact permission still did not prove the mailbox was deliverable, so an eligible lead could reach send without provider-backed address evidence | Migration `0180` adds a distinct Hunter secret binding, verification balance/readiness state, address status/score/check time, conservative lead states, new suppressions, and verification receipts. Connection uses Hunter's free usage read without retaining account identity. Adding a lead explicitly may consume one company-owned check; pending is pollable; HTTP 222 remains retryable unknown; only `valid` becomes eligible; risk states stay review-required; invalid, disposable, and 451 privacy claims suppress globally; and sends reject checks older than 30 days. Provider keys use `X-API-KEY`; source URLs, raw responses, identities, credentials, and queried addresses are omitted or redacted. Twelve focused validator/provider/database/UI tests pass; shared, database, and UI typechecks, migration numbering/safety, and the production UI build pass. Server typecheck emits no Outreach diagnostic and reaches only the two unrelated recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no Outreach file listed. Isolated 3105 applied `0180`, returned healthy/auth-ready, and direct schema inspection proved the exact defaults and checks while 3103/3104 stayed up and 3100 stayed down. No credential, Hunter/Postmark call, credit, email, spend, deploy, or main-runtime mutation occurred. | `packages/db/src/migrations/0180_company_outreach_address_verification.sql`, `server/src/services/hunter-company-outreach-provider.ts`, `server/src/services/company-outreach.ts`, `server/src/routes/company-outreach.ts`, `server/src/__tests__/company-outreach.test.ts`, `ui/src/pages/CompanyOutreach.tsx`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | No-resend Outreach stale-state reconciliation implemented and source-canaried | A process interruption could leave `executing` or delayed `submitted` messages unresolved, with no bounded automatic way to distinguish a safe provider read from a dangerous resend | The existing startup/domain-scheduler loop now scans at most 25 unchanged messages older than 15 minutes. Stale `executing` becomes explicit outcome-unknown with a durable receipt and no replay authority. Stale `submitted` is read only when an exact Postmark message ID already exists; unchanged results are throttled through `updatedAt`, final delivery/bounce writes a receipt, and bounce enters global suppression. Missing/revoked authority and provider errors cannot send. The embedded integration proves a 20-minute execution reservation is quarantined while a known-ID submission settles delivered with one system-bound secret read. Focused provider/database tests pass; server typecheck emits no Outreach diagnostic and reaches only the two unrelated recovery timestamp errors. The isolated 3105 runtime restarted healthy/auth-ready with the scheduler path installed; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced during rotation. Ports 3103/3104 stayed up and 3100 stayed down. No credential, provider call, email, spend, deploy, or main-runtime mutation occurred. | `server/src/services/company-outreach.ts`, `server/src/index.ts`, `server/src/__tests__/company-outreach.test.ts`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Non-spending Outreach trust expiry implemented and source-canaried | The send gate rejected Hunter checks older than 30 days, but the board surface could still label the lead eligible until that failed send | Migration `0181` binds each lead's latest verification result to the exact Outreach connection. Startup and the existing domain tick move expired `valid` leads to `verification_required`, preserve the historical Hunter status/time/score, record a zero-credit expiry receipt, and never invoke Hunter. Reverification remains a board action. The embedded integration proves exactly one 31-day result expires, keeps the exact connection, blocks send, and writes `verificationCreditConsumed: false`. All 12 focused validator/provider/database/UI tests and shared/database/UI typechecks pass; migration numbering/safety pass; server typecheck emits no Outreach diagnostic and reaches only the unrelated recovery pair. Isolated 3105 applied `0181`, direct schema inspection proved its UUID foreign key, and health/auth returned ready. The known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced during rotation. Ports 3103/3104 stayed up and 3100 stayed down. No credential, provider call, credit, email, spend, deploy, or main-runtime mutation occurred. | `packages/db/src/migrations/0181_company_outreach_verification_expiry.sql`, `packages/db/src/schema/company_outreach.ts`, `server/src/services/company-outreach.ts`, `server/src/index.ts`, `server/src/__tests__/company-outreach.test.ts`, `ui/src/pages/CompanyOutreach.tsx`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Deterministic no-resend customer-reply reconciliation implemented and source-canaried | Gmail and Slack replies reserved execution before provider mutation, but a process loss after submission could leave the customer response stuck in `executing` with no bounded read-only recovery path | Migration `0182` admits a durable `reply_reconciled` receipt. Gmail sends now carry one deterministic RFC Message-ID derived from the Summon message UUID; Slack already carries that UUID as `client_msg_id`. Startup and the existing scheduler tick inspect replies unchanged for 15 minutes, resolve the exact secret as `company-inbox-reconciler`, and read only the exact mailbox/workspace/thread key. A proven provider match becomes `sent`; missing, duplicate, mismatched, unauthorized, or unavailable evidence becomes `outcome_unknown`, never a resend. Two provider tests prove both exact lookup contracts without calling send endpoints. The embedded-Postgres test proves one stale Gmail reply recovers, one unproven Slack reply quarantines, both exact system secret reads occur, two receipts persist, and neither send adapter runs. All 15 focused tests pass; shared/database typechecks and migration checks pass; server typecheck emits no Inbox diagnostic and reaches only the unrelated Recovery pair. Isolated 3105 applied `0182`, direct schema inspection proved the new receipt constraint, scheduler startup returned healthy with zero companies, and the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced during rotation. Ports 3103/3104 stayed up and 3100 stayed down. No credential, provider request, customer message, spend, deploy, or main-runtime mutation occurred. | `packages/db/src/migrations/0182_company_inbox_reply_reconciliation.sql`, `packages/db/src/schema/company_inbox.ts`, `server/src/services/gmail-company-inbox-provider.ts`, `server/src/services/slack-company-inbox-provider.ts`, `server/src/services/company-inbox.ts`, `server/src/index.ts`, `server/src/__tests__/company-inbox.test.ts`, `doc/COMPANY-INBOX.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | No-replay Stripe Payment Link reservation reconciliation implemented and source-canaried | Stripe creation used stable idempotency keys, but a timeout or process loss after provider acceptance could leave an offer unresolved; replay after Stripe prunes a key can create a second public price | Migration `0183` adds `outcome_unknown` to offers and a `payment_link_reconciled` receipt. Ambiguous create errors remain reserved. Startup and the existing scheduler inspect reservations unchanged for 15 minutes, resolve the exact secret as `company-payment-link-reconciler`, and use GET only to scan the bounded complete Payment Link collection. Recovery requires one exact Summon UUID plus matching mode, safe URL, policy flags, one line item, Price metadata/amount/currency/interval, and Product metadata/name/description. One proof recovers active/inactive state; absence, duplicate, mismatch, missing authority, provider error, or scan limit quarantines and blocks replay. The 11 focused tests prove one recovered reservation, one quarantined reservation, two audited system secret reads, no create call during reconciliation, and immediate ambiguous-error reservation. Shared/database/UI typechecks, migration checks, and the production UI build pass; server typecheck emits no Payments diagnostic and reaches only the unrelated Recovery pair. Isolated 3105 applied `0183`, direct schema inspection proved both constraints, returned healthy/bootstrap/auth ready with zero companies, and made no provider call. The known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced during rotation. Ports 3103/3104 stayed up and 3100 stayed down. No credential, Stripe request, payment link, charge, refund, spend, deploy, or main-runtime mutation occurred. | `packages/db/src/migrations/0183_company_payment_link_reconciliation.sql`, `packages/db/src/schema/company_payments.ts`, `packages/shared/src/types/company-payments.ts`, `server/src/services/stripe-company-payments-provider.ts`, `server/src/services/company-payments.ts`, `server/src/index.ts`, `server/src/services/stripe-company-payments-provider.test.ts`, `server/src/__tests__/company-payments.test.ts`, `doc/COMPANY-PAYMENTS.md`, `FEATURE-PARITY.md` |

| 2026-08-29 | Authenticated exact-current Stripe Refund/Dispute webhook reconciliation implemented and source-canaried | Signed Checkout events created fulfillment work, but refund and dispute lifecycle changes still depended on manual sync and an out-of-order event could not safely update current customer-protection state | Migration `0184` admits `refund_webhook_reconciled` and `dispute_webhook_reconciled` receipts with one partial unique account/type/event-ID index. The raw-body Stripe signature now admits only three Refund and five Dispute signals. Each mapped event resolves the exact `operations.credential`, re-verifies account/mode, reads the exact current object, validates local refund metadata plus PaymentIntent/amount/currency, and stores only redacted provider truth. Completed duplicates do not re-read; transient failures remain retryable with 503; provider mismatch becomes review; unmapped external refunds are identity-free ignored receipts; older dispute delivery cannot regress a newer provider state. Seventeen focused server/provider/database tests and one Payments page test pass; shared/database/UI typechecks, migration numbering/safety, and the production UI build pass. Server typecheck emits no Payments diagnostic and reaches only the two unrelated Recovery timestamp errors. The token gate remains the exact inherited 52/32/18 baseline with no Payments finding. Isolated 3105 applied `0184`, direct schema inspection proved both receipt types and the unique index, and health returned OK with zero companies while 3103/3104 stayed up and 3100 stayed down. The known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced during rotation. No credential, Stripe call, payment, refund, dispute mutation, spend, deployment, or main-runtime mutation occurred. | `packages/db/src/migrations/0184_company_payment_operations_webhooks.sql`, `packages/db/src/schema/company_payments.ts`, `packages/shared/src/types/company-payments.ts`, `server/src/services/stripe-company-payment-webhook.ts`, `server/src/services/stripe-company-payments-provider.ts`, `server/src/services/company-payment-webhooks.ts`, `server/src/routes/company-payment-webhooks.ts`, `server/src/__tests__/company-payments.test.ts`, `ui/src/pages/CompanyPayments.tsx`, `doc/COMPANY-PAYMENTS.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | No-replay initial Vercel deployment reconciliation implemented and source-canaried | Initial deployment already reserved one approval-correlated receipt, but a timeout or process interruption could leave it `executing` with no safe automatic proof and no safe retry | Deployment creation now carries the approval UUID as provider metadata. Timeout, transport, conflict, rate-limit, and server ambiguity stay reserved. After 15 minutes the existing startup/scheduler loop resolves the exact Website token and uses GET only to inspect at most five complete production-deployment pages for one exact correlation, project, target, and approved ref or SHA. One match recovers provider state; zero, duplicate, mismatch, missing authority, provider failure, or bounded-scan uncertainty becomes terminal `outcome_unknown`, blocks replay, and cannot overwrite a newer release. All 12 focused provider and embedded-database tests pass, including two-page exact recovery, duplicate quarantine, one recovered reservation, one unproven quarantine, audited system secret access, and no second create call. Server typecheck emits no Website diagnostic and reaches only the two unrelated Recovery timestamp errors. Isolated 3105 restarted healthy with zero companies and the scheduler installed while 3103/3104 stayed up and 3100 stayed down. No credential, Vercel call, deployment, spend, or main-runtime mutation occurred. | `server/src/services/vercel-deployment-provider.ts`, `server/src/services/company-website.ts`, `server/src/index.ts`, `server/src/services/vercel-deployment-provider.test.ts`, `server/src/__tests__/company-website.test.ts`, `doc/COMPANY-WEBSITE.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | No-republish Zernio social-post reconciliation implemented and source-canaried | Direct board actions generated server-side post IDs and provider ambiguity became a false terminal failure, so an HTTP retry could create a second public post after Zernio's short request-key window expired | Migration `0185` adds `outcome_unknown` plus `post_reconciled`. A direct board action now supplies a UUID that survives failed responses, binds exact instructions, becomes Zernio's request key and `summonPostId` metadata, and returns the same reservation on retry. Timeout, transport, rate-limit, and server ambiguity remain `executing`. After 15 minutes the existing startup/scheduler loop resolves the exact profile credential and uses GET only across at most five complete profile/time pages. Recovery requires one exact metadata, copy, timezone, schedule, and destination match; zero, duplicate, mismatch, missing authority, provider failure, or incomplete evidence quarantines without republishing. Eleven focused validator/provider/database/UI tests pass, including two-page recovery, duplicate-correlation failure, exact HTTP retry with no second create, one recovered reservation, one quarantine, audited system secret access, and visible no-republish copy. Shared/database/UI typechecks, migration checks, and UI build pass; server typecheck emits no Social diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no Social finding. Isolated 3105 applied `0185`, started healthy/auth-ready with zero companies and the scheduler installed while 3103/3104 stayed up and 3100 stayed down. No credential, Zernio request, social post, spend, deployment, or main-runtime mutation occurred. | `packages/db/src/migrations/0185_company_social_post_reconciliation.sql`, `packages/shared/src/types/company-social.ts`, `packages/shared/src/validators/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/index.ts`, `server/src/services/zernio-company-social-provider.test.ts`, `server/src/__tests__/company-social.test.ts`, `ui/src/pages/CompanySocial.tsx`, `doc/COMPANY-SOCIAL.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Authenticated durable Zernio lifecycle webhook reconciliation implemented and source-canaried | Social post truth still depended on manual refresh or stale-post recovery; duplicate or out-of-order notifications had no durable admission boundary, and trusting a delivery payload could regress current provider state | Migration `0186` adds encrypted webhook authority, provider readiness evidence, `partial` publication state, unique event-keyed receipts, and a durable queue that stores no raw body, copy, or provider metadata. Connection provisions exactly the scheduled, published, partial, failed, and cancelled signals with a generated 256-bit secret and requires a test fire. The public route verifies HMAC-SHA256 over the exact raw body plus agreeing event UUIDs, durably admits the minimal notification, and returns immediately. Startup and periodic processing atomically reserve or reclaim work, resolve the exact credential, GET and validate the exact current post, prevent terminal-state regression, retry transient reads with bounded backoff, and transactionally write one event-keyed receipt. Eleven focused server tests and two Social page tests pass; shared/database/UI typechecks, migration checks, and the production UI build pass. Server typecheck emits no Social diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no Social finding. Isolated 3105 applied `0186`, remained healthy/auth-ready with zero companies after repeated scheduler intervals, and installed startup plus periodic processing while 3103/3104 stayed up and 3100 stayed down. No credential, Zernio call, social post, spend, deployment, or main-runtime mutation occurred. | `packages/db/src/migrations/0186_company_social_webhooks.sql`, `packages/db/src/schema/company_social.ts`, `packages/shared/src/types/company-social.ts`, `server/src/services/zernio-company-social-webhook.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/routes/company-social-webhooks.ts`, `server/src/__tests__/company-social.test.ts`, `server/src/services/zernio-company-social-webhook.test.ts`, `server/src/services/zernio-company-social-provider.test.ts`, `ui/src/pages/CompanySocial.tsx`, `ui/src/pages/CompanySocial.test.tsx`, `doc/COMPANY-SOCIAL.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Automatic exact-current Stripe economic refresh implemented and source-canaried | Revenue, cash, ARR, profitability, runway, and the market-cap proxy depended on a founder pressing sync; the read stopped at the latest 100 records, and stale or failed truth could remain visually plausible | Migration `0187` adds durable due time, processing lease, retry/error state, freshness, failure count, sanitized error, snapshot fingerprint, and automatic success/failure receipts. Connection is immediately due; startup and the existing 15-minute scheduler reserve bounded accounts sequentially; authenticated non-ignored Stripe events accelerate the next refresh. Each attempt audits the exact system secret read, re-inspects account/mode, paginates current-month paid Checkout Sessions and active subscriptions in 100-record pages through a hard 10-page ceiling, and updates freshness without emitting an unchanged receipt. Interrupted leases recover after 10 minutes, transient failures back off from one minute to one hour for at most eight failures, and terminal authority/account failures stop. Payments and Mission Control expose as-of/next/error state. Market Cap and Aether never price or aggregate test, bounded, stale, error, unavailable, foreign-currency, or unpriced evidence. Twenty focused Payment/provider/dashboard tests, ten portfolio tests, twenty-two shared tests, and nine focused UI tests pass; shared/database/UI typechecks, migration checks, `git diff --check`, and the production UI build pass. Server typecheck emits no Payments/dashboard/portfolio diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no changed financial file listed. Isolated 3105 applied only `0187`, returned healthy/auth-ready with zero companies, and made no provider request. Port 3104 retained PID 38240; 3100 and 3103 were already down and remained down. No credential, Stripe call, money movement, spend, deployment, or main-runtime mutation occurred. | `packages/db/src/migrations/0187_company_payment_revenue_refresh.sql`, `packages/db/src/schema/company_payments.ts`, `packages/shared/src/types/company-payments.ts`, `packages/shared/src/types/dashboard.ts`, `packages/shared/src/vitals-market-cap.ts`, `server/src/services/stripe-company-payments-provider.ts`, `server/src/services/company-payments.ts`, `server/src/services/company-payment-webhooks.ts`, `server/src/services/dashboard.ts`, `server/src/services/aether-portfolio.ts`, `server/src/index.ts`, `ui/src/pages/CompanyPayments.tsx`, `ui/src/components/MissionControl.tsx`, `ui/src/pages/MarketCap.tsx`, `doc/COMPANY-PAYMENTS.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | One-constraint Verified Cure Loop implemented and source-canaried | Aether could create one task containing every finding and the board had to rerun diagnosis and choose the next task manually; this violated the one-constraint company doctrine and made continuous self-improvement either babysat or theatrical | The board can start a continuous loop pinned to the exact current Lightmark artifact. Its version-3 receipt selects one highest-impact finding, one exact workspace, and one accountable Engineering-first employee while retaining all baseline IDs only for regression detection. A completed task triggers one coalesced read-only diagnosis through startup or the existing scheduler. A next task exists only when a strictly newer artifact proves the exact target disappeared. Unchanged, regressed, lost, failed, cancelled, or ownership-incomplete evidence pauses durably; a perfect scan completes; terminal events prevent repeated scans. The prompt explicitly forbids evidence fabrication, security weakening, unrelated fixes, wake, deploy, publish, send, and spend. An embedded-Postgres acceptance proves first-target selection, exact-proof advancement, no duplicate while waiting, pause on unchanged evidence, explicit board restart, and completion at 100/100. Eleven focused server tests, two shared validator tests, and five UI tests pass; shared/UI typechecks and the production UI build pass. Server typecheck emits no Aether diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no Aether finding. The current-source 3105 canary restarted healthy/auth-ready with zero companies and performed no diagnosis or provider run; 3104 retained PID 38240 while 3100 and 3103 remained down. Repository-wide `git diff --check` still reports only pre-existing whitespace in unrelated Claude/Grok handoff files. No credential, agent wake, deployment, publication, message, spend, provider call, or protected-runtime mutation occurred. | `packages/shared/src/types/aether-portfolio.ts`, `packages/shared/src/validators/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `server/src/index.ts`, `server/src/__tests__/aether-portfolio-cure-loop.test.ts`, `ui/src/pages/AetherPortfolio.tsx`, `FEATURE-PARITY.md` |

| 2026-08-29 | Continuous read-only Plaid cash and operating-expense evidence implemented and source-canaried | The last partial economic-parity row still relied on manually imported statements and Stripe cash, so expenses and runway could not remain current without founder babysitting | Migration `0188` adds company-owned Plaid connections, encrypted secret bindings, hash-only external identities, a private sync cursor, transaction tombstones, cash/expense snapshots, leases, bounded retry, and changed/recovery/failure receipts. Board-only Hosted Link requires business-account attestation and exactly one Item. Startup plus the existing 15-minute scheduler apply added/modified/removed transaction patches and real-time balance reads. Only posted USD debits count; pending, credits, income, transfers, loan payments, and foreign currencies are excluded. Fresh complete production bank evidence replaces imported-statement rows while retaining explicit board entries, so the same bank transaction is not counted twice. Sandbox, stale, error, incomplete, and currency-mismatched evidence cannot prove profit or runway. Five provider, two embedded-database, eight dashboard/portfolio, and five UI tests pass; shared/database/UI typechecks, migration checks, and production build pass. Server typecheck emits no finance diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no new finding. The isolated 3105 canary applied `0188`, returned health `ok` and Aether portfolio `200` with zero companies, and made no provider call; 3104 retained PID 38240 while 3100/3103 remained down. Repository-wide `git diff --check` remains blocked only by pre-existing unrelated Claude/Grok handoff whitespace. No credential, Plaid/Stripe request, money movement, spend, deployment, message, or protected-runtime mutation occurred, so the row is blocked only by real production evidence. | `packages/db/src/migrations/0188_company_finance_connections.sql`, `packages/db/src/schema/company_finance.ts`, `packages/shared/src/types/company-finance.ts`, `server/src/services/plaid-company-finance-provider.ts`, `server/src/services/company-finance.ts`, `server/src/routes/company-finance.ts`, `server/src/services/dashboard.ts`, `server/src/services/aether-portfolio.ts`, `server/src/index.ts`, `ui/src/components/CompanyFinanceConnections.tsx`, `ui/src/pages/Costs.tsx`, `ui/src/components/MissionControl.tsx`, `doc/finance/PLAID-CONNECTION.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Fail-closed Plaid disconnect and revocation reconciliation implemented | Continuous bank evidence had no board-controlled exit, so a local “revoked” label could not prove that Plaid access and subscription billing had actually ended | Migration `0189` adds explicit processing/retrying/error/confirmed revocation state, a recoverable lease, bounded backoff, provider-confirmation time, and evidence receipts. Board disconnect first revokes local reads and clears future syncs, then calls Plaid `/item/remove`. Only success or documented already-absent states confirm removal; invalid credentials remain an error. Interrupted and transient attempts reconcile through the existing scheduler. Provider confirmation atomically disables generated tokens and deletes connection bindings while retaining historical mirror/receipts. The UI requires a deliberate confirmation and exposes every outcome. Twenty-five focused server tests and six focused UI tests pass; shared/database/UI typechecks, migration checks, scoped diff checks, and production build pass. Server typecheck reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no changed finance file listed. Isolated 3105 applied only `0189`, preserved Finance Preview, returned health `ok` and Costs `200`, exposed zero inert connections, and made no provider call; protected 3104 remained PID 38240. No live credential or Plaid call occurred, so a production disconnect/reconnect drill remains evidence-blocked. | `packages/db/src/migrations/0189_company_finance_revocation.sql`, `packages/db/src/schema/company_finance.ts`, `packages/shared/src/types/company-finance.ts`, `server/src/services/plaid-company-finance-provider.ts`, `server/src/services/company-finance.ts`, `server/src/routes/company-finance.ts`, `server/src/index.ts`, `ui/src/components/CompanyFinanceConnections.tsx`, `doc/finance/PLAID-CONNECTION.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Founder verdicts connected to the next company diagnosis | Summon captured granular positive/negative output feedback, but the data stayed inside the feedback/export subsystem and could not influence prioritization | Company Loop now exposes a company-scoped rolling 30-day verdict summary and supplies at most five recent negative signals to the Cofounder's next-three task. Each reason is normalized, capped at 240 characters, and explicitly labeled untrusted evidence rather than a command. No vote wakes an agent or creates work; the existing board accept/reject gate still controls execution. Twenty focused service/UI tests, shared/UI typechecks, the production build, scoped diff check, and an actual 3105 API canary pass. Server typecheck reaches only the same two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no Company Loop finding. Finance Preview truthfully returned zero positive, zero negative, and zero recent signals; protected 3104 remained PID 38240. No external credential, provider call, task proposal, agent wake, message, deploy, spend, or protected-runtime mutation occurred. | `packages/shared/src/types/company-loop.ts`, `server/src/services/company-loop.ts`, `server/src/services/company-loop.test.ts`, `ui/src/components/CompanyLoop.tsx`, `ui/src/components/CompanyLoop.test.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Board-controlled always-on customer support loop implemented and source-canaried | Summon could sync Gmail or Slack and convert a message into owned work only after a founder opened the Inbox; Polsia's deployed client exposed explicit support-inbox start, pause, run-now, status, cadence, and prompt controls | Migration `0190` adds paused/active state, cadence, bounded operating context, board authorization, next/last/success times, a recoverable lease, failure state, and a partial due index to the existing connector. Startup and the existing scheduler reserve due connectors, system-resolve the exact credential, sync a bounded inbound window, create at most one high-priority task and assigned receipt per message, recover a failed wake without duplication, and back off before pausing after eight failures. Connector edits/revocation pause it. The compact Inbox surface exposes Configure, Start/Pause, Run now, cadence, action, freshness, and failure state while stating that replies remain separate drafts/approvals/board sends. Eighteen focused tests pass; shared/database/UI typechecks, migration checks, and the production UI build pass. Server typecheck emits no Inbox diagnostic and reaches only the unrelated Recovery pair. Token gates remain the inherited 52/32/18 baseline with no Inbox finding. Isolated 3105 applied `0190`, exposed all eleven fields plus the due index, returned healthy/auth-ready with zero connectors, and loaded both new control routes; protected 3104 remained PID 38240. No live credential, provider request, customer reply, task in the preview database, spend, deployment, or protected-runtime mutation occurred. | `packages/db/src/migrations/0190_company_inbox_automation.sql`, `packages/db/src/schema/company_inbox.ts`, `packages/shared/src/types/company-inbox.ts`, `packages/shared/src/validators/company-inbox.ts`, `server/src/services/company-inbox.ts`, `server/src/routes/company-inbox.ts`, `server/src/index.ts`, `server/src/__tests__/company-inbox.test.ts`, `ui/src/api/companyInbox.ts`, `ui/src/pages/CustomerInbox.tsx`, `doc/COMPANY-INBOX.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Board-controlled always-on outreach preparation implemented and source-canaried | Summon had permission-based campaigns and exact per-message approval, but the founder still had to open Outreach and initiate every preparation cycle; Polsia's deployed client exposes start, pause, run-once, cadence, instructions, status, and prospect controls | Migration `0191` adds campaign-level paused/active state, Daily/Weekdays/Weekly cadence, bounded board context, board authorizer, next/last/success times, a ten-minute recoverable lease with stable run UUID, failure backoff, eight-failure auto-pause, last task, and a partial due index. Startup and the existing scheduler create one deterministic high-priority task for the accountable Sales owner and dispatch the normal assignment wake. The task may review only existing permission-qualified leads and prepare an exact approval request; it explicitly forbids scraping or importing contacts, consuming Hunter credits, calling Postmark, spending, or sending. The compact Outreach surface exposes Configure, Start/Pause, Run now, cadence, last/next state, last task, and failure truth while repeating the exact-recipient/exact-copy board boundary. Eight focused shared/server/UI tests pass; shared/database/UI typechecks, migration checks, scoped diff checks, and the production UI build pass. Server typecheck emits no Outreach diagnostic and reaches only the unrelated Recovery pair. Token gates remain the inherited 52/32/18 baseline with no Outreach finding. Isolated 3105 applied only `0191`, exposed all twelve automation columns and the due index, and returned health/auth ready. One explicitly labeled, provider-free preview sender and paused Weekdays campaign were then seeded so the controls are inspectable; the sender is in error state, action buttons are disabled, and the page returns 200. Protected 3104 remained PID 38240. The expected graceful-drain `ECONNRESET` reproduced during rotation. No live credential, provider call, verification credit, email, agent wake, task, spend, deployment, or protected-runtime mutation occurred, so live Postmark/Hunter evidence remains required. | `packages/db/src/migrations/0191_company_outreach_automation.sql`, `packages/db/src/schema/company_outreach.ts`, `packages/shared/src/types/company-outreach.ts`, `packages/shared/src/validators/company-outreach.ts`, `server/src/services/company-outreach.ts`, `server/src/routes/company-outreach.ts`, `server/src/index.ts`, `server/src/__tests__/company-outreach.test.ts`, `ui/src/api/companyOutreach.ts`, `ui/src/pages/CompanyOutreach.tsx`, `doc/COMPANY-OUTREACH.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Exact-linked Aether portfolio command view implemented and source-canaried | Summon could measure portfolio economics, site health, setup, and continuity, but the board could not see the real open task queue, recurring routines, or recent activity across all formed Aether companies in one place; Polsia's current client exposes portfolio task, recurring-task, report, analytics, and terminal-log reads | The private Aether response now includes one deduplicated operations projection over exact-linked companies only: active/in-progress/review/blocked counts, up to twelve ordered open tasks, active routines, and ten recent allowlisted activity titles. Finished work and ambiguous/unmapped companies are excluded; blocked and review work sort first; task/routine links remain company-scoped; raw activity details are never projected; loading the page never wakes an employee. Sixteen focused server/UI tests pass, including duplicate-company suppression, terminal/unlinked exclusion, ordering, activity-detail redaction, links, and visible no-wake language. Shared and UI typechecks, the production build, scoped diff check, and exact inherited token baseline 52/32/18 pass. Server typecheck reaches only the two unrelated Recovery timestamp errors. A fresh isolated source runtime on 3100 applied all 190 migrations, returned health/auth ready and 47 artifact companies, exposed the new operations shape with truthful zero pre-formation counts, and served `/portfolio` as 200. Protected 3104 retained PID 38240 and the existing 3105 preview retained PID 58056. Browser-harness visual acceptance remains paused behind Helium's one-time remote-debugging consent. No company formation, task, routine, event, wake, provider call, message, deployment, or spend occurred. | `packages/shared/src/types/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `server/src/services/aether-portfolio.test.ts`, `ui/src/pages/AetherPortfolio.tsx`, `ui/src/pages/AetherPortfolio.test.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company-scoped grounded Help Center implemented and built | The account menu still opened Paperclip engine documentation, while Polsia's current public help outcome combines guide navigation, FAQ, search, and grounded questions inside its own product | Summon now searches 28 original operating guides locally with no provider call or credits, including the daily Nightshift, scheduled-media, board-notification preference, safe company-lifecycle, and governed Deep Repair contracts. Results explain the real operating boundary, link to the selected company's exact control, and distinguish product help from the company's Customer Inbox. The shared router prefixes `/help`, the account menu opens it internally, and no Polsia copy, client source, or assets are included. The focused search/page/menu/router tests pass; UI typecheck and production build pass; scoped diff check is clean. Token gates remain the inherited 52/32/18 baseline with no Help finding. Existing source previews can serve the rebuilt client; Helium visual acceptance remains paused behind its one-time remote-debugging consent. No credential, credit, company mutation, agent wake, provider call, message, deployment, spend, or protected-runtime mutation occurred. | `ui/src/lib/summon-help.ts`, `ui/src/pages/HelpCenter.tsx`, `ui/src/components/SidebarAccountMenu.tsx`, `ui/src/lib/company-routes.ts`, `ui/src/App.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Governed recurring Company Media implemented and source-canaried | Polsia's current Media Library guidance exposes one-off and Daily, Weekdays, or Weekly scheduled image/video generation, while Summon had only accountable one-off media tasks | Migration `0193` adds one Image and one Video schedule per company with a standing brief, purpose, format, optional bounded video duration, accountable owner, optional project, board authorizer, next/last-success state, ten-minute lease, bounded backoff, and eight-failure auto-pause. A newly saved schedule is inert and paused; editing an active schedule preserves its status; Start attempts the first ordinary task immediately; Pause clears due state; Run now uses the saved contract. Startup and the existing scheduler reserve due work, skip same-kind active overlap, retain one deterministic issue across an interrupted wake, and fail closed for paused companies or ineligible owners. Every task requires one real attached artifact and explicitly forbids publication, provider expansion, credit purchase, messaging, or advertising. Seventeen focused shared/server/UI/help tests pass; shared/database/UI typechecks and migration numbering/safety pass; server typecheck reaches only the two unrelated Recovery timestamp errors. An isolated 3106 source canary saved and read back one paused Weekdays Image schedule under a zero-budget disposable designer, served the Media route, and created no issue. It was stopped while protected 3100/3104/3105 retained their PIDs. No wake, provider call, artifact, publication, message, deployment, or spend occurred. | `doc/COMPANY-MEDIA.md`, `packages/db/src/migrations/0193_company_media_schedules.sql`, `packages/db/src/schema/company_media.ts`, `packages/shared/src/types/company-media.ts`, `packages/shared/src/validators/company-media.ts`, `server/src/services/company-media.ts`, `server/src/routes/company-media.ts`, `server/src/__tests__/company-media.test.ts`, `ui/src/api/companyMedia.ts`, `ui/src/pages/CompanyMedia.tsx`, `ui/src/pages/CompanyMedia.test.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |
| 2026-08-29 | Board-authorized recurring Nightshift implemented and source-canaried | Summon's “daily autonomous company cycle” parity claim only generated a brief; it did not automatically start bounded company work, while Polsia's current Night Task FAQ says a nightly cycle may run one to five tasks | Migration `0192` adds one durable company schedule with active/paused state, UTC hour, bounded duration, one-to-five work slots, dollar ceiling, next/last/success state, ten-minute lease, failure backoff, and eight-failure auto-pause. The existing startup/scheduler tick starts due schedules without overlap, and the board can Configure, Start/Save, Pause, or Run saved schedule now. Manual and scheduled Nightshifts share hard task-count and dollar dispatch ceilings. Fifty-five focused shared/server/UI/help tests pass; shared/database/UI typechecks and migration checks pass; server typecheck reaches only the two unrelated Recovery timestamp errors. Isolated 3106 persisted and read back a future 02:00 UTC four-hour schedule with three work slots and a $5 ceiling, then paused it with no cycle created; the dashboard returned 200 and the canary stopped while 3100/3104/3105 were untouched. No agent wake, provider call, message, deployment, or spend occurred. During the first canary attempt an invalid config-source label caused source startup to fall back to Adam's default local Paperclip database; it applied the pending source migration set through `0192`, reconciled built-in agents, reaped two orphaned heartbeat runs, and generated daily briefs for eight companies before being stopped. No protected preview PID changed and no external provider call was observed. The config is corrected and the isolated acceptance is the evidence-bearing run. | `packages/db/src/migrations/0192_company_nightshift_schedules.sql`, `packages/db/src/schema/company_loop.ts`, `packages/shared/src/types/company-loop.ts`, `packages/shared/src/validators/company-loop.ts`, `server/src/services/company-loop.ts`, `server/src/routes/company-loop.ts`, `server/src/index.ts`, `ui/src/components/CompanyLoop.tsx`, `ui/src/lib/summon-help.ts`, `doc/execution-semantics.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Founder-controlled operating-alert preferences implemented and source-canaried | Polsia lets founders choose which recurring company emails they receive, while Summon's governed notification rail was all-or-nothing once a destination had been authorized | Migration `0194` adds one board-owned per-company allowlist over Summon's seven fixed operating-event kinds. Existing companies default virtually to all seven. Disabled activity is not captured; queued pending or failed deliveries are revoked before provider submission; delivery rechecks the setting before reservation. Preferences never create delivery authority, recall already-submitted requests, or add product-update or marketing messages. The seven controls live inside Customer Inbox's Board notifications section, and Help documents the boundary. Twenty-one focused shared/server/UI/help tests pass; shared/database/UI typechecks, migration numbering/safety, and production build pass. Server typecheck has no notification diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no new preference file finding; scoped whitespace is clean. Isolated 3106 applied `0194`, proved the seven-event default, saved and read back exactly three enabled kinds, loaded the Customers route, and reported zero destinations, deliveries, or provider calls. It was stopped while protected 3100/3104/3105 retained PIDs 33584/38240/58056; 3106 is closed and 3105 still returns the Customers page with HTTP 200. No credential, notification, message, wake, deployment, spend, or protected-runtime mutation occurred. | `doc/COMPANY-NOTIFICATIONS.md`, `packages/db/src/migrations/0194_company_notification_preferences.sql`, `packages/db/src/schema/company_notifications.ts`, `packages/shared/src/types/company-notifications.ts`, `packages/shared/src/validators/company-notifications.ts`, `server/src/services/company-notifications.ts`, `server/src/routes/company-notifications.ts`, `server/src/__tests__/company-notifications.test.ts`, `ui/src/api/companyInbox.ts`, `ui/src/components/CompanyNotificationPreferences.tsx`, `ui/src/pages/CustomerInbox.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |
| 2026-08-29 | Safe company lifecycle management implemented, built, and source-canaried | Summon exposed rename, archive, status mutation, and a raw one-click permanent-delete path in separate places, but it lacked Polsia's coherent Company Settings lifecycle outcome and did not protect permanent deletion from active work or generic resume from bypassing a budget hard stop | Company Settings now presents Pause, Resume, Archive, Restore, and Permanent delete as one contract. Manual pause records reason and time; resume clears it; budget pauses reject generic resume; archived companies remain explicitly selectable for restoration. Permanent deletion is board-only and company-scoped, requires paused or archived state, exact typed name, and zero active runs or wakes, then reuses the existing atomic cleanup cascade. The UI and durable lifecycle guide state that deletion removes Summon control-plane data and local credential records only; provider accounts, repositories, deployed sites, subscriptions, and local workspaces remain owner-controlled. Thirty focused server tests, one shared validator test, and 23 focused UI/context/help tests pass; shared/UI typechecks, production UI build, and Storybook static build pass. Server typecheck reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no lifecycle component or story finding. Isolated 3106 created one named canary company, verified manual pause, rejected a wrong confirmation with 422, accepted the exact name, and returned 404 afterward. It was stopped while protected 3100/3104/3105 retained PIDs 33584/38240/58056; 3105 still returns the Customers route with HTTP 200. No external resource, provider call, message, wake, deployment, or spend was touched. | `doc/COMPANY-LIFECYCLE.md`, `packages/shared/src/validators/company.ts`, `server/src/services/companies.ts`, `server/src/routes/companies.ts`, `server/src/__tests__/companies-service.test.ts`, `server/src/__tests__/company-branding-route.test.ts`, `ui/src/components/CompanyLifecycleControls.tsx`, `ui/src/pages/CompanySettings.tsx`, `ui/src/context/CompanyContext.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |
| 2026-08-29 | Governed Website Deep Repair implemented, built, and source-canaried | Summon could run a standard repair and separately govern production deployment, but it had no distinct bounded escalation for a recurring live bug that a normal fix did not solve; Polsia documents that outcome as Ultra Fix | Deep Repair is board-only and binds one live ready Website, accountable owner, completed/blocked/cancelled source repair, affected production page, reproduction steps, expected/actual behavior, recurrence evidence, fix-only attestation, typed confirmation, and an explicit USD recorded-spend ceiling. Migration `0195` enforces one active repair per Website and one task per request UUID. The ordinary task inherits source goal/workspaces, requires reproduce-before, root cause, smallest fix, regression test, and before/after evidence, and cannot deploy or broaden scope. Heartbeat admission blocks the next run at recorded issue spend, and recovery treats the cap as an expected blocked state. Focused shared/server/UI/help and recovery tests pass; shared/database/UI typechecks, migration checks, and production build pass; server typecheck reaches only the two unrelated Recovery timestamp errors; token gates remain the inherited 52/32/18 baseline with no Deep Repair finding. Isolated 3106 applied `0195`, exposed both indexes, served the Website UI, projected `NIG-2` at 2,700/2,500 cents with zero remaining, returned 409 on a wake, recorded skipped `issue.spend_limit` evidence, and created zero heartbeat/provider runs. After the recovery refinement, one scheduler interval created zero further wake receipts. The canary stopped while protected 3100/3104/3105 retained PIDs 33584/38240/58056. No credential, external provider request, deployment, publication, message, credit purchase, or real spend occurred. | `doc/COMPANY-WEBSITE.md`, `packages/db/src/migrations/0195_company_website_deep_repairs.sql`, `packages/db/src/schema/issues.ts`, `packages/shared/src/types/company-website.ts`, `packages/shared/src/validators/company-website.ts`, `server/src/services/company-website-deep-repair.ts`, `server/src/services/issue-spend-limit.ts`, `server/src/services/heartbeat.ts`, `server/src/services/recovery/service.ts`, `server/src/routes/company-website.ts`, `server/src/__tests__/company-website.test.ts`, `server/src/__tests__/heartbeat-process-recovery.test.ts`, `ui/src/components/CompanyWebsiteDeepRepair.tsx`, `ui/src/pages/CompanyWebsite.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |

| 2026-08-29 | Exact board-started task result notifications implemented, built, and source-canaried | Polsia's current FAQ says Run Now task completion, failure/rejection, and cancellation notify the owner while automatic tasks do not send the Run Now email; Summon had terminal task state in the UI but no equivalent exact outbound result class | Migration `0196` adds `manual_task_result` as an eighth board preference while preserving existing saved allowlists. A `done`, `blocked`, or `cancelled` transition qualifies only through its exact same-company issue-linked heartbeat run and board-requested wake (or legacy on-demand source); cancellation binds the exact cancelled run. Automatic, unrelated, unchanged, forged-detail, and context-mismatched updates stay silent. Fixed copy omits task titles/details and reuses the existing exact-destination, connector/activity dedupe, pre-submit reservation, and no-replay contract. Five focused files pass 25 tests; shared/database/UI typechecks, migration checks, production build, and scoped whitespace pass. Server typecheck reaches only the two unrelated Recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no changed notification UI finding. Isolated 3106 applied only `0196`, accepted and read back all eight kinds, restored the prior exact three-kind setting, served Customers with HTTP 200, and showed zero destinations or deliveries before being stopped. Protected 3100/3104/3105 retained PIDs 33584/38240/58056. No credential, Slack call, notification, message, wake, deployment, or spend occurred. | `doc/COMPANY-NOTIFICATIONS.md`, `packages/db/src/migrations/0196_company_notification_manual_task_results.sql`, `packages/db/src/schema/company_notifications.ts`, `packages/shared/src/types/company-notifications.ts`, `server/src/services/company-notifications.ts`, `server/src/__tests__/company-notifications.test.ts`, `ui/src/components/CompanyNotificationPreferences.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |

| 2026-08-29 | Governed published social-post deletion implemented and source-canaried without a live provider | Polsia exposes deletion of a published Tweet; Summon could cancel scheduled work but intentionally had no published-post deletion contract | Migration `0197` separates irreversible published deletion from scheduled cancellation. A board request binds one exact provider post and UUID before one DELETE. Success and provider absence settle `deleted`; ambiguous transport/provider outcomes become `deletion_outcome_unknown`; repeats never replay; and the startup/periodic scheduler GETs only the exact post after 15 minutes to prove absence or restore the prior state for a new explicit request. Stale webhooks and ordinary refresh cannot overwrite deletion state, and the Social page hides stale public links while explaining the boundary. Sixteen focused validator/provider/server/UI tests pass; shared/database/UI typechecks, migration numbering/safety, and production UI build pass. Server typecheck has no Social finding and reaches only the two unrelated Recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no Social finding; scoped whitespace is clean. Isolated 3106 applied only `0197`, exposed the four deletion columns and three updated constraints, stayed healthy across a full scheduler interval, and contained zero Social connections, posts, or receipts. It stopped with 3106/54335 closed while protected 3100/3104/3105 retained PIDs 33584/38240/58056. No credential, Zernio call, public-post mutation, message, deployment, spend, or protected-runtime mutation occurred. | `doc/COMPANY-SOCIAL.md`, `packages/db/src/migrations/0197_company_social_published_post_deletion.sql`, `packages/db/src/schema/company_social.ts`, `packages/shared/src/types/company-social.ts`, `packages/shared/src/validators/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/routes/company-social.ts`, `server/src/__tests__/company-social.test.ts`, `ui/src/api/companySocial.ts`, `ui/src/pages/CompanySocial.tsx`, `FEATURE-PARITY.md` |

| 2026-08-29 | Exact-post social analytics implemented and source-canaried without a live provider or add-on purchase | Polsia exposes social performance inside the operating dashboard, while Summon could publish, reconcile, receive signed lifecycle events, and delete but retained no exact current performance evidence | Migration `0198` adds bounded canonical totals and per-target analytics, freshness/attempt/retry/error state, one due index, and `post_analytics_refreshed` receipts. Manual reads are board/accountable-owner only under `channels.read`; automatic reads reserve at most 25 due published/partial posts and call only Zernio's exact `GET /v1/analytics?postId=...`. Returned post and target identities must exactly match the immutable local snapshot. Fresh evidence refreshes daily; 202 remains pending; transient failure waits one hour; 402 becomes visible unavailable state with no retry and `purchaseAttempted: false`. Raw copy, media, URLs, audience identity, and provider payloads are not stored; canonical comparison prevents order-only duplicate receipts. Focused provider/service/UI tests, shared/database/UI typechecks, migration numbering/safety, and production UI build pass. Server typecheck emits no Social diagnostic and reaches only the two unrelated Recovery timestamp errors; token gates remain the inherited 52/32/18 baseline with no Social finding. A fresh 3106/54336 runtime applied all migrations through `0198`, returned health/auth ready, exposed all six analytics columns, both analytics/receipt constraints and the due index, then stayed at zero companies, runs, wakes, Social rows, receipts, and provider-log matches across a full scheduler cycle before both ports closed. Two earlier launch attempts used malformed canary config metadata; the loader silently returned defaults while the banner still named the requested path, so source touched Adam's default local instance and launched local Grok workers. Both exact process trees were stopped. Incident-created open runs/wakeups/leases and agent states were restored; SUM-317 returned to Operations / `in_progress`; one generated system recovery comment/action was removed; due issue, daily briefs, migrations, and the failed-run audit record were preserved. Final read-only verification found zero open incident runs, wakes, or leases and all nine agent states restored. No person-to-person message, public post, deployment, or spend was observed; zero model usage is not claimed. | `packages/db/src/migrations/0198_company_social_post_analytics.sql`, `packages/db/src/schema/company_social.ts`, `packages/shared/src/types/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/routes/company-social.ts`, `server/src/index.ts`, `server/src/services/zernio-company-social-provider.test.ts`, `server/src/__tests__/company-social.test.ts`, `ui/src/api/companySocial.ts`, `ui/src/pages/CompanySocial.tsx`, `ui/src/pages/CompanySocial.test.tsx`, `doc/COMPANY-SOCIAL.md`, `FEATURE-PARITY.md` |

| 2026-08-29 | Exact destination-specific social copy implemented, built, and visually accepted without a live provider | Polsia's social composer supports adapting one post for different networks, while Summon previously sent one caption to every selected destination | Company Social now keeps one immutable default plus an optional exact override for each selected channel. Validation rejects duplicate or unselected overrides; employee approvals and receipts bind SHA-256 destination manifests; board request replay conflicts on any tailored-copy change. The Zernio adapter uses documented per-platform `customContent`, and both stale-create recovery and signed-event rereads fail closed unless the provider echoes the exact destination/copy pair. Twenty-three focused validator, provider, service, and UI tests pass; shared/UI typechecks and production UI build pass. Server typecheck reaches only the two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with no Company Social finding; scoped whitespace is clean. Helium rendered the Polsia-style composer and tailored-copy summary at 2134px and 390×844 with zero root or dialog overflow; the mobile dialog fit without internal scrolling. Isolated 3107/54337 stopped cleanly and protected 3100 retained PID 16764. No credential, Zernio request, public post, message, deployment, spend, or protected-runtime mutation occurred. | `packages/shared/src/types/company-social.ts`, `packages/shared/src/validators/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/__tests__/company-social.test.ts`, `ui/src/api/companySocial.ts`, `ui/src/pages/CompanySocial.tsx`, `ui/src/pages/CompanySocial.test.tsx`, `doc/COMPANY-SOCIAL.md`, `FEATURE-PARITY.md` |

| 2026-08-29 | Governed social image/video staging implemented, built, and visually accepted without a live provider | Polsia's social composer supports media, while Summon previously governed text, deletion, lifecycle receipts, and analytics but could not bind a private company artifact to the exact provider post | Migration `0199` adds an immutable media snapshot over the same-company attachment/asset identity, kind, content type, byte size, SHA-256, filename, and allowlisted public provider URL. Validation permits up to four images or one video, never mixes kinds, caps each file at 100 MiB, and limits scheduled media to Zernio's seven-day temporary retention. Execution rereads exact private bytes, checks size/type/hash, obtains one presigned grant from the fixed provider origin, uploads only to an allowlisted R2 host, and never persists the signed URL. Provider create, GET-only ambiguity recovery, and signed-event reread require the exact ordered media manifest; staging failure before create is deterministic and cannot be mistaken for a maybe-published post. Twenty-five focused tests pass; shared/database/UI typechecks, migration numbering/safety, and production UI build pass. Server typecheck has no Social diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain at the exact inherited 52/32/18 baseline with no Company Social finding. A fresh isolated 3108/54338 runtime applied all migrations through `0199`, served one synthetic private image artifact and media receipt, and made no provider call. Helium selected the exact artifact at 2134px and 390×844; desktop/mobile dialogs stayed fully reachable with zero horizontal overflow. The canary stopped while protected 3100 retained PID 16764. No credential, Zernio request, public post, message, deployment, spend, or protected-runtime mutation occurred. | `doc/COMPANY-SOCIAL.md`, `packages/db/src/migrations/0199_company_social_media.sql`, `packages/db/src/schema/company_social.ts`, `packages/shared/src/types/company-social.ts`, `packages/shared/src/validators/company-social.ts`, `server/src/services/zernio-company-social-provider.ts`, `server/src/services/company-social.ts`, `server/src/routes/company-social.ts`, `server/src/__tests__/company-social.test.ts`, `server/src/services/zernio-company-social-provider.test.ts`, `ui/src/api/companySocial.ts`, `ui/src/pages/CompanySocial.tsx`, `ui/src/pages/CompanySocial.test.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Governed Zernio social DMs/comments implemented, built, and visually accepted without a live provider | Polsia says it replies to customers, and Zernio's current first-party API now exposes existing inbox conversations plus direct-message and organic-comment replies; Summon had governed Gmail/Slack conversations and governed social publication but no social customer-conversation rail | Company Inbox now accepts one exact Zernio profile/secret binding, verifies the profile, projects bounded inbound DMs and organic comments, and stores only safe account/thread/message/type evidence. Drafts bind one exact synchronized inbound item. Execution can reply only inside the existing conversation or to the exact parent comment; it cannot initiate, auto-reply, react, like, hide, delete, or private-reply from a public comment. DMs use the Summon message UUID as Zernio's idempotency key. Ambiguity becomes `outcome_unknown`; the stale reconciler performs exact-current reads and settles only one exact owner reply, with zero resend. The board-controlled support loop may poll and create internal owned work but cannot send externally. Twenty-five focused provider/shared/embedded/UI assertions pass; shared/UI typechecks and the production build pass. Server typecheck has no Inbox/Zernio diagnostic and reaches only the two unrelated Recovery timestamp errors. Token gates remain at the inherited 52/32/18 baseline with no Company Inbox finding. Isolated 3109/54339 rendered two synthetic social conversations, an active 15-minute support loop, exact authority, secret custody, receipts, and `Sync Zernio`. Helium proved desktop and 390×844 layouts with no root overflow or false eligibility warning. Both canary ports closed while protected 3100 retained PID 16764. No live credential, Zernio request, customer reply, person-to-person message, publication, deployment, spend, or protected-runtime mutation occurred. | `doc/COMPANY-INBOX.md`, `doc/COMPANY-SOCIAL.md`, `packages/shared/src/validators/company-inbox.ts`, `server/src/services/zernio-company-inbox-provider.ts`, `server/src/services/zernio-company-inbox-provider.test.ts`, `server/src/services/company-inbox.ts`, `server/src/routes/company-inbox.ts`, `server/src/__tests__/company-inbox.test.ts`, `ui/src/pages/CustomerInbox.tsx`, `ui/src/pages/CustomerInbox.test.tsx`, `FEATURE-PARITY.md` |

| 2026-08-29 | Company Stack reduced from three Secrets-page detours to one governed activation action | Polsia's product hides infrastructure activation behind one company experience, while Summon's owner-custodied rail required the founder to leave Stack, create or identify three secrets, return, and map each one manually | The Stack dialog now selects the Engineering-like owner, accepts GitHub, Neon, and Vercel tokens directly or reuses saved secrets, encrypts pasted values, clears all raw browser fields before provisioning, and passes only the resulting IDs into the existing board-only service. Deterministic `400`/`401`/`403`/`404`/`409`/`422` rejection removes only secrets created by that attempt; ambiguous transport or `5xx` preserves encrypted custody and instructs explicit saved-secret recovery. Six focused UI tests pass, including raw-field clearing before a held-open provider call, three-secret cleanup on `422`, and retention on `502`; UI typecheck and production build pass. Token gates remain the inherited 52/32/18 baseline with no Stack finding. Helium rendered the desktop and 390×844 top/action states with no horizontal overflow and a reachable mobile action after internal scroll. The isolated 3109/54339 canary returned health `ok` with exactly zero stacks, then both ports closed; the known Windows Ctrl-C heartbeat-drain `ECONNRESET` reproduced, and protected 3100 retained PID 16764. No credential was entered and no GitHub, Neon, or Vercel request, resource, spend, deployment, or protected-runtime mutation occurred. Live provider acceptance and OAuth onboarding remain unclaimed. | `ui/src/pages/CompanyStack.tsx`, `ui/src/pages/CompanyStack.test.tsx`, `doc/COMPANY-STACK.md`, `FEATURE-PARITY.md` |

| 2026-08-29 | Company Stack exact-read recovery implemented, migration-tested, and visually accepted without a live provider | The direct-connect slice retained credentials after ambiguous failure but the existing provisioning service returned the same failed Stack idempotently, so “retry through Saved secrets” was not a working recovery path | Migration `0200` adds recovery receipts. One board-only UUID action requires the exact typed Stack name and attestation, claims failed/partial/infrastructure-unknown state atomically, and reads exact GitHub repository, Neon project/branch/database/role/connection, Vercel project/GitHub linkage, and sensitive `DATABASE_URL` metadata before resuming. A missing resource is created only after definite absence; absence or an inconclusive read after a prior ambiguous mutation never authorizes replay; conflicting identity stops; source-publication ambiguity remains on its separate GitHub reconciliation path; and no recovery path deletes owner resources. Completed and safely stopped UUIDs are idempotent. Twenty focused provider/server/UI tests pass; shared/database/UI typechecks, migration checks, and the production build pass. Server typecheck has no Stack diagnostic and reaches only two unrelated Recovery timestamp errors. Token gates remain the inherited 52/32/18 baseline with neither changed Stack UI file listed. A fresh isolated 3109/54339 runtime applied every migration through `0200`, exposed one synthetic `outcome_unknown` Stack with three exact bindings and zero provider calls, and rendered the disabled confirmation dialog at desktop and 390×844 with zero root/dialog overflow and a reachable mobile action. No confirmation was submitted and no GitHub, Neon, Vercel, source, deployment, spend, or protected-runtime mutation occurred. Live provider/OAuth acceptance remains unclaimed. | `packages/db/src/migrations/0200_company_stack_recovery.sql`, `packages/shared/src/validators/company-stack.ts`, `server/src/services/portable-company-stack-provider.ts`, `server/src/services/company-stack.ts`, `server/src/routes/company-stack.ts`, `server/src/services/portable-company-stack-provider.test.ts`, `server/src/__tests__/company-stack.test.ts`, `ui/src/api/companyStack.ts`, `ui/src/pages/CompanyStack.tsx`, `ui/src/pages/CompanyStack.test.tsx`, `work/company-stack-recovery-canary-20260829/stack-recovery-dialog-desktop.png`, `work/company-stack-recovery-canary-20260829/stack-recovery-dialog-mobile.png`, `doc/COMPANY-STACK.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | All 47 Aether companies formed and exact-linked in an isolated current-source acceptance | The portfolio formation rail was source-complete but had never crossed its explicit board confirmation, so every company remained an artifact-only preview and no real cure could be owned | The guarded request formed 47 of 47 selected local repositories with zero failures: 47 manual-mode companies at the legacy `$0` company value, 47 projects, 47 exact verified workspaces, 376 pending Core-8 employees, 47 pending `staff_formation` decisions, and 47 paused built-in Reflection Coaches. Later audit established that `$0` means no ceiling, not zero spend; none of these records has been silently rewritten. The one-company staffing approval now repairs that legacy boundary by enforcing at least the complete selected employee-cap total as a company hard stop before any seat activation. There were zero heartbeat and live runs. An identical replay formed zero duplicates and returned all 47 as already connected. The acceptance found and fixed a cure-readiness defect: a paused built-in employee could be selected because the filter required assignability but not invokability. It now requires both, and the regression proves pending Engineering plus paused Reflection Coach yields no owner until Engineering becomes active. The corrected snapshot reports 45 `needs_agent_approval`, one `not_needed`, one `scan_failed`, and zero false-ready cures. Focused service coverage passes 11 tests. Helium rendered `/LIG/portfolio` at 1440×1000 and exact 390×844 with 47/47 linked, truthful $0 expenses, all other financial outcomes unproven, no alerts, and zero root/body overflow; the expanded mobile row exposes `Approve Core-8`. The isolated 3110/54340 canary remains live for preview. Protected 3100 remained PID 16764. No employee activation, cure issue, wake, credential, provider call, message, deployment, publication, or spend occurred. | `server/src/services/aether-portfolio.ts`, `server/src/services/aether-portfolio.test.ts`, `FEATURE-PARITY.md`, `work/aether-formation-canary-20260829/portfolio-formed-desktop.jpg`, `work/aether-formation-canary-20260829/portfolio-formed-mobile.jpg` |
| 2026-08-29 | Exact portfolio-to-Core-8 decision handoff and board-readable formation card implemented and visually accepted | The formed portfolio exposed `Approve Core-8` as a status label but supplied no exact action; cure-task links also inherited the currently selected company, and the destination decision dumped raw seat JSON | Private cure receipts now include the exact linked company prefix and pending formation approval ID. The portfolio offers `Review Core-8 formation`, links existing cure tasks through the owning prefix, and falls back only to that company's pending approvals list when an exact card is absent. The formation detail presents one plain-language decision, the company hard stop beside the $80/month employee-cap total, and eight compact seat rows; raw payload remains collapsed. Later audit corrected the company-level boundary: the existing manual `$0` value is uncapped, so approval itself now visibly enforces at least the selected employee total as a company hard stop before activation while preserving a larger existing ceiling. Twenty-seven focused server/shared/UI assertions and Shared/UI typechecks pass. Token gates remain the inherited 52/32/18 baseline with no changed file implicated. Helium clicked the `anchormarianas.com` row through `/ANC/approvals/<exact-id>`, saw one pending decision and one Approve control, then rendered the redesigned card at desktop and exact 390×844 with zero alerts or root/body overflow. The 3110/54340 canary remains live and protected 3100 remains untouched. No approval, employee activation, cure issue, wake, provider call, credential, message, deployment, publication, or spend occurred. | `packages/shared/src/types/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `ui/src/pages/AetherPortfolio.tsx`, `ui/src/pages/AetherPortfolio.test.tsx`, `ui/src/components/ApprovalPayload.tsx`, `ui/src/components/ApprovalPayload.test.tsx`, `work/aether-formation-canary-20260829/core8-decision-desktop.jpg`, `work/aether-formation-canary-20260829/core8-decision-mobile.jpg`, `FEATURE-PARITY.md` |
| 2026-08-29 | Ranked Aether staffing-decision queue implemented and verified against the live 47-company canary | Polsia puts `Hire Your AI Employee` at the top of the founder workspace, while Summon's exact Core-8 decisions were discoverable only after expanding individual portfolio rows | A new portfolio-level queue derives only exact `needs_agent_approval` receipts, orders them by portfolio rank, opens one exact company approval, and contains no bulk-approve mutation. The live 3110 API reports 45 eligible decisions and selects rank 2 `anchormarianas.com` at the exact ANC approval; the perfect company and failed scan are excluded. Ten focused UI tests pass, including exact ordering/linking, non-pending exclusion, no `Approve all`, and zero-state hiding. UI typecheck and production build pass. Token gates remain the inherited 52/32/18 baseline with no Aether Portfolio finding. Fresh desktop/mobile visual acceptance is explicitly pending because Helium revoked remote-debugging permission before capture. The canary remains healthy, protected 3100 retains PID 16764, and no approval, employee activation, cure issue, wake, credential, provider call, message, deployment, publication, or spend occurred. | `ui/src/pages/AetherPortfolio.tsx`, `ui/src/pages/AetherPortfolio.test.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | PostgreSQL data export proved through a real disposable snapshot, restore, and delete acceptance | The database snapshot rail was migration-tested but still lacked proof that a founder could obtain and independently restore an actual data artifact on this Windows source runtime | The 3112/54330 isolated runtime created a board-only custom-format dump through the product API using official portable `pg_dump` 18.6. Acceptance exposed and fixed a source portability boundary: `sslmode=disable` is now accepted only for explicit loopback databases, rejects channel binding there, and remains rejected for remote hosts. The clean 986,907-byte receipt and download shared SHA-256 `05af9bb21bcf3e92b108285584e906bbf4dcbc514ce2bc4f5c3e1569d6ff94c0`; headers were `application/vnd.postgresql.custom-dump` and private/no-store. Catalog inspection found the synthetic marker, a fresh database restore returned the exact marker and one company, that database was dropped, board deletion removed the stored object, and the next download returned 404. Ten focused provider/snapshot tests pass; server typecheck reaches only the two unrelated Recovery timestamp errors. No external provider, credential, spend, deployment, publication, message, or protected runtime was used. | `server/src/services/postgres-database-snapshot-provider.ts`, `server/src/services/postgres-database-snapshot-provider.test.ts`, `server/src/__tests__/company-stack-database-snapshots.test.ts`, `work/database-snapshot-canary-20260829/acceptance.mjs`, `doc/COMPANY-STACK.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Summon board MCP negotiated and read the live 47-company canary | Polsia documents MCPs inside its agents but no official source exposes a control-plane endpoint, while Summon's own board MCP had not been recorded as live evidence in the parity ledger | A read-only stdio JSON-RPC acceptance against `http://127.0.0.1:3110/api` negotiated protocol `2025-06-18`, returned server `summon` version `1.0.0`, listed exactly eight bounded company/task/agent/fleet tools, and read all 47 formed Aether companies with manual mode, active lifecycle, and zero recorded spend. No create, comment, stop, wake, approval, employee activation, provider, message, deployment, publication, or spend mutation was invoked. The broader authenticated Paperclip MCP package remains the formal agent task API. | `scripts/summon-mcp-server.mjs`, `.mcp.json`, `packages/mcp-server`, `FEATURE-PARITY.md` |
| 2026-08-29 | Botvillage optimistic wake and truthful live-role interaction completed in source | The visual office already walked and posted optimistically, but a paused villager kept showing `Zzz`, there was no arrival reaction, and the slim card had no truthful assistant/user/tool signal from Summon's own runtime | Sending now wakes the selected villager locally before the durable post, preserves movement and wake on failure, produces a short-lived user reaction on arrival, and reconciles the selected card against current rooms. Current role bubbles/cards derive only from active-run `currentToolName`, `lastAssistantSnippet`, or initiating-comment evidence; they reveal neither tool names nor transcript snippets. The slim card uses the employee's actual icon and title plus a clipped two-line goal. Twenty-one focused Company World/Layout tests pass; UI typecheck and the production build pass. Token gates remain exactly at the inherited 52/32/18 baseline with no Company World finding; scoped whitespace is clean. The live 3110 preview remains healthy. Fresh Helium desktop/mobile acceptance is pending because remote-debugging permission is still revoked. No prompt, comment, wake, provider call, approval, employee activation, spend, deployment, publication, message, or protected-runtime mutation occurred. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/pages/FactoryFloor.tsx`, `ui/src/index.css`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Local Board Chat attachment-to-response boundary accepted end to end | File controls and source tests existed, but no acceptance proved that the chat subprocess could read exact company-owned bytes and return a durable response | A disposable 3113/54341 current-source runtime created one $0 company and standing chat, uploaded a 156-byte text file, then the isolated local board subprocess fetched it through `/api/attachments/:id/content`. Upload, download, subprocess receipt, and durable assistant turn agreed on SHA-256 `8b67fd306b6e20a5966c22d5ac3b00d390578a9f816cc2f9b6fef1a0ed4ef21f`; SSE emitted `start`, `chunk`, `done`; both turns persisted; zero heartbeat runs and zero spend were recorded. No external provider was called. The audit also corrected an overclaim: current Board Chat invokes a local Claude concierge rather than the configured Cofounder adapter. Canary ports 3113/54341 are closed and 3110 remains healthy. Governed voice remains blocked pending separately authorized provider acceptance. | `work/board-chat-attachment-canary-20260829/acceptance.mjs`, `work/board-chat-attachment-canary-20260829/acceptance-result.json`, `work/board-chat-attachment-canary-20260829/fake-claude-receipt.json`, `FEATURE-PARITY.md`, `DESIGN.md` |
| 2026-08-29 | Company Chat stopped impersonating the configured Cofounder | The immediate responder was the hardcoded local Claude concierge, while classic/immersive chat headers, the welcome, durable reply bubbles, and Product Help presented it as the real Cofounder employee | Both chat surfaces now say `Board Concierge`; Chat Mode says `local · company-scoped`; sentinel replies never inherit the CEO agent's name or icon; the welcome no longer claims employee authorship; and Help preserves the boundary between immediate company direction and accountable employee execution. Ten focused Board Chat tests pass, UI typecheck and production build pass, and the token gate remains the inherited 52/32/18 baseline with none of the changed files listed. Fresh desktop/mobile visual acceptance remains pending Helium permission. No prompt, comment, wake, provider call, credential, spend, deployment, publication, message, or protected-runtime mutation occurred. | `ui/src/pages/BoardChat.tsx`, `ui/src/pages/BoardChat.test.tsx`, `ui/src/pages/ChatMode.tsx`, `ui/src/lib/summon-help.ts`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company World stopped drawing ghost employees and made sleep evidence visible | Every open department rendered a villager despite having no employee, while paused employees calculated `Zzz` text that CSS kept invisible; there was no evidence-based long-quiet rule | Villagers now require a real employee. Paused employees visibly sleep; available/waiting employees sleep only after a real heartbeat is at least one hour old; missing history is not guessed stale; live tool/assistant/user activity wins; and optimistic prompt travel still wakes the target locally before delivery. Twenty-three focused Company World/Layout tests, UI typecheck, and production build pass. The token gate stays at the inherited 52/32/18 baseline with neither changed Company World file listed. Fresh desktop/mobile visual acceptance remains pending Helium permission. No prompt, comment, wake, provider call, credential, spend, deployment, publication, message, approval, employee activation, or protected-runtime mutation occurred. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/index.css`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Pending Core-8 seats stopped appearing as hired office employees | The isolated portfolio correctly retained 376 seats behind exact board decisions, but the digital office classified those database records as available staff and Company World drew them as villagers | `pending_approval` is now a first-class office state. Both office projections say `Pending approval`; Mission Control treats the seat as configured rather than online; Company World retains the house but draws no villager and disables prompting until approval. Live 3110 roster truth for `anchormarianas.com` is eight pending seats, one paused Reflection Coach, and zero active employees. Thirty-one focused Company World/Office/Mission Control/Layout tests, UI typecheck, and production build pass. The token gate remains the inherited 52/32/18 baseline with none of the changed office files listed. Fresh desktop/mobile visual acceptance remains pending Helium permission. No decision, employee activation, task, comment, wake, provider call, credential, spend, deployment, publication, message, or protected-runtime mutation occurred. | `ui/src/components/CompanyOffice.tsx`, `ui/src/components/CompanyOffice.test.tsx`, `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/components/MissionControl.tsx`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Live villagers gained distinct home-to-work movement | Company World exposed truthful live state but every employee remained at one visual coordinate and merely bobbed, so a growing run did not produce the requested walk-to-work behavior | Every approved employee now has a visible work spot. Non-live employees stay by the house; authoritative working state changes a transform-only travel variable, moves the same villager in four steps over 650ms, and animates the nested sprite at the station. Mobile retains the contract with a smaller distance; reduced-motion removes travel and all sprite animation. Thirty-two focused Company World/Office/Mission Control/Layout tests, UI typecheck, and production build pass, including a real live-issue-to-work-spot assertion. The token gate remains the inherited 52/32/18 baseline with none of the changed Company World files listed. Fresh desktop/mobile visual acceptance remains pending Helium permission. No task, comment, wake, provider call, credential, spend, deployment, publication, message, approval, activation, or protected-runtime mutation occurred. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `ui/src/index.css`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company Media stopped presenting pending or paused seats as runnable employees | The live formed-company shape has eight pending Core-8 seats and one paused Reflection Coach, while the media dialog offered every non-terminated record; choosing a pending seat produced a late API conflict, and choosing a paused seat contradicted the dialog's immediate-wake claim | One-off media now lists only approved, invokable employees with a valid organization chain and shows an explicit approve-or-resume boundary when none exist. Schedule configuration may retain a paused accountable employee, but `Save & start` is disabled and names the employee that must be resumed; pending, terminated, and invalid-chain seats remain absent. Fourteen focused shared/server/UI media tests, UI typecheck, and production build pass. The token gate remains the inherited 52/32/18 baseline with no Company Media file listed. Fresh visual acceptance remains pending Helium permission. No media task, schedule, wake, provider call, credential, spend, publication, approval, activation, or protected-runtime mutation occurred. | `ui/src/pages/CompanyMedia.tsx`, `ui/src/pages/CompanyMedia.test.tsx`, `doc/COMPANY-MEDIA.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Mission Control and Company Office stopped counting pending seats as employees | Company World drew no pending villagers and office rooms said `Pending approval`, but the Polsia-style Team list still named those records as team members and both Mission Control and Company Office included them in the employee total | Team now lists approved employees only and presents pending formation as one explicit Formation link. Business and Company Office use the same approved-only employee count while retaining a separate pending-seat total. Read-only live ANC evidence remains one paused approved Reflection Coach, eight pending Core-8 seats, and zero active employees. Fourteen focused Mission Control/Office/Media tests, UI typecheck, and production build pass. The token gate remains the inherited 52/32/18 baseline with none of the changed dashboard or office files listed. Fresh visual acceptance remains pending Helium permission. No decision, approval, activation, task, wake, provider call, credential, spend, publication, or protected-runtime mutation occurred. | `ui/src/components/MissionControl.tsx`, `ui/src/components/MissionControl.test.tsx`, `ui/src/components/CompanyOffice.tsx`, `ui/src/components/CompanyOffice.test.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Routine ownership and Run now stopped treating formation records as executable employees | Routine creation/detail and the run dialog filtered only terminated agents; a pending seat could be saved as the default, and a pending or paused default satisfied the client-side Run-now admission check until the server rejected it | Added one shared staffing contract: approved assignable employees may own saved work; approved invokable employees with a valid organization chain may run now. Routine creation/editing use the first boundary, while Run now uses the second, disables submission for stale ineligible defaults, and names the exact approve/resume/replace/org-repair action. Company Media reuses the same functions. Twenty-four focused staffing/Routine-list/Run-dialog/Media tests, UI typecheck, and production build pass. Removing an obsolete fixture color improves the token-gate baseline to 51/32/18, with none of the changed source or remaining changed test files listed. Fresh visual acceptance remains pending Helium permission. No routine, task, wake, provider call, credential, spend, approval, activation, publication, or protected-runtime mutation occurred. | `ui/src/lib/agent-staffing.ts`, `ui/src/lib/agent-staffing.test.ts`, `ui/src/pages/Routines.tsx`, `ui/src/pages/RoutineDetail.tsx`, `ui/src/components/RoutineRunVariablesDialog.tsx`, `ui/src/components/RoutineRunVariablesDialog.test.tsx`, `ui/src/pages/CompanyMedia.tsx`, `FEATURE-PARITY.md` |
| 2026-08-29 | Global navigation stopped competing with the Polsia-style company workspace | The dense Mission Control and gamified office existed, but the surrounding Paperclip sidebar still promoted Chat, Messages, generic Inbox, Audit, Watchtower, Projects, every Agent, Roadmap, and Activity as peer destinations; mobile replaced the company model with Agents and generic Inbox | Desktop first-level navigation is now exactly Mission Control, Company Office, Tasks, Decisions, Org, Costs, and Settings. Mobile is Control, Office, Create, Tasks, and Decide. Search and contextual links keep infrequent routes reachable, extension outlets remain intact, and no route or contract was deleted. Thirty-three focused sidebar/navigation tests, UI typecheck, and production build pass. The token gate remains the inherited 51/32/18 baseline with neither changed navigation source/test file listed. Fresh visual acceptance remains pending Helium permission. No task, decision, provider call, credential, spend, approval, activation, publication, deployment, or protected-runtime mutation occurred. | `ui/src/components/Sidebar.tsx`, `ui/src/components/Sidebar.test.tsx`, `ui/src/components/MobileBottomNav.tsx`, `ui/src/components/MobileBottomNav.test.tsx`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company Office stopped switching from a business simulation into a restaurant metaphor | The map showed real employees in an office, but opening its queue changed the product into a kitchen: Head Chef, The Pass, dishes, cooking, burners, plated work, tickets, and a classic-Inbox exit | The queue and detail drawer now use one company vocabulary: Boardroom and department work areas, tasks, Active, Awaiting review, Working, employee notes, evidence, and a direct Tasks exit. One pure copy regression pins the vocabulary and exact work-area map. The same pass replaced the file's raw white literal and all arbitrary 10/11px declarations with semantic foreground and locked nano/micro tokens, improving the inherited token baseline from 51/32/18 to 50/22/8 with no Factory Floor finding left. Twenty focused Company Office/World/Mission Control tests, UI typecheck, and production build pass. Fresh visual acceptance remains pending Helium permission. No task, decision, comment, wake, provider call, credential, spend, approval, activation, publication, deployment, or protected-runtime mutation occurred. | `ui/src/pages/FactoryFloor.tsx`, `ui/src/pages/FactoryFloor.copy.test.ts`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company World employee inspection reduced to the botvillage slim-card contract | The fixed card repeated the selected room, state, and task ID, so clicking a villager still opened a control-plane status panel instead of the tiny identity-and-intent card in the supplied brief | A deterministic helper normalizes the current task title to at most three words and 28 characters. The staffed card now contains only profile image, name, title, short goal, and truthful last-role chip; room, state, and task ID duplication are removed, while unstaffed positions get one concise staffing sentence. Twenty-one focused Company World/Office/Mission Control tests, UI typecheck, and production build pass. Token gates remain at the improved 50/22/8 baseline with no Company World finding. The 3110 preview remains healthy; fresh desktop/mobile visual acceptance is pending Helium permission. No task, decision, comment, wake, provider call, credential, spend, approval, activation, publication, deployment, message, or protected-runtime mutation occurred. | `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Company World preserved every work area around the fixed work-radio overlay | The actual 3110 render showed that the required middle-bottom radio covered the lower-center area at 1280×720 and hid the Operations, Support, and Legal row at 390×844; the wide `you · appearance not chosen` pill also obscured the center seat | The fixed no-scroll scene now reserves the radio footprint inside the plot grid. The board remains visually unchosen as the exact `you` nametag, and focus lifts the building plus outlines its nametag instead of boxing the whole plot. Read-only browser measurements prove nine plots, zero prompt intersections, zero body overflow on both axes, 46.27px desktop and 37.26px mobile clearance, plus a fully contained 352×190 fixed card at 1280×720 and 390×844. Thirty-six focused Company World/Office/Layout/Mission Control tests, UI typecheck, and production build pass. Token gates remain at 50/22/8 with no changed component finding. No task, decision, comment, wake, provider call, credential, spend, approval, activation, publication, deployment, message, or protected-runtime mutation occurred. | `ui/src/index.css`, `ui/src/components/CompanyWorld.tsx`, `ui/src/components/CompanyWorld.test.ts`, `DESIGN.md`, `FEATURE-PARITY.md` |

| 2026-08-29 | Aether formation replaced its false `$0` guardrail with a real Core-8 company ceiling | Formation assigned eight $10 employee caps but created the containing company at zero, while the authoritative budget contract and runtime checks define zero as no limit; the confirmation copy incorrectly promised a `$0 monthly budget` prevented spend | Future formations now use and activity-log an exported $80 company ceiling equal to the complete Core-8 employee total. Portfolio economics carries per-company ceiling evidence, capped/uncapped totals, exact zero-as-unlimited language, and a pre-approval warning. Missing fields from the older 3110 server fail closed as `runtime refresh required` instead of crashing or inventing a value. The 47 existing canary companies were not mutated; live 3110 renders the warning without horizontal overflow at desktop or 390px. Mission Control separately labels zero-run reliability `Unproven`, never `0%`. Forty-two focused tests, shared/UI typechecks, and the production build pass; the server typecheck reaches only two unrelated Recovery timestamp errors. Token gates remain at the inherited 50/22/8 baseline with no changed file implicated. No company budget, employee, task, approval, wake, provider call, credential, spend, deployment, publication, or message was changed. | `packages/shared/src/vitals-formation.ts`, `packages/shared/src/types/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `ui/src/pages/AetherPortfolio.tsx`, `ui/src/components/MissionControl.tsx`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-29 | Core-8 approval now enforces the company hard stop before activation | The live 47-company canary correctly retained its legacy `$0` value, but the future approval transaction still had no company-level stop and the decision card falsely said zero blocked provider spend | The staffing approval derives the complete selected employee-cap total, creates or raises an active hard-stop company policy before any seat activation, preserves a larger board-set ceiling, and then applies each employee cap. Approved-but-incomplete retries reconcile missing side effects; rejection creates no budget or activation. An embedded-database acceptance proved a disposable legacy-zero company became capped at $80, eight seats became idle, and the approval receipt retained prior/enforced budget evidence. Forty-two focused tests, shared/UI typechecks, and the production build pass; server typecheck reaches only the two unrelated Recovery timestamp errors. Read-only browser acceptance on the real pending ANC card proves `Company hard stop`, `At least $80.00 / month`, the separate employee total, absent false `$0` copy, and no horizontal overflow at 1280×720 or 390×844. The live decision remained pending and no live record was changed. Token gates remain at the inherited 50/22/8 baseline with no approval file implicated. No approval, employee activation, task, wake, provider call, credential, spend, deployment, publication, or message occurred. | `server/src/services/approvals.ts`, `server/src/services/formation-seed.ts`, `server/src/__tests__/approvals-staff-formation.test.ts`, `server/src/__tests__/companies-service.test.ts`, `ui/src/components/ApprovalPayload.tsx`, `ui/src/components/ApprovalPayload.test.tsx`, `FEATURE-PARITY.md`, `DESIGN.md` |
| 2026-08-29 | Stable DNS absence became an evidence-bound company diagnosis instead of a dead scan state | The real Aether artifact reported `book.movie` as `getaddrinfo ENOTFOUND`, leaving the 47th company without a score, finding, or improvement prompt even though public unavailability was its clearest business constraint | Summon now recognizes only stable hostname-not-found evidence, projects 0/F with one Public availability scorecard and one 100-impact reliability finding, and serves a deterministic prompt read-only from the retained report. The prompt marks the error untrusted, separates repository work from domain/DNS/hosting/deploy authority, and requires a newer full public diagnosis. Timeouts and ambiguous network failures remain `scan_failed`. Direct current-source evaluation against the real artifact reports 47 successful, zero failed, 76.79 average, one perfect, and 46 prompts; `book.movie` receives `public-site-unreachable-f8280795210c2eb0.md`. Thirteen service tests and fourteen UI tests pass; shared/UI typechecks pass; server typecheck reaches only the two inherited Recovery timestamp errors. A fresh current-source 3114/54329 canary with one disposable local routing company rendered the exact totals and expanded `book.movie` row. The prompt button reached `Copied`; desktop 1280×720 and mobile 390×844 both had equal body/root client and scroll widths, and the mobile row plus prompt control remained contained. The prompt file does not exist on disk because reading must not rewrite Lightmark evidence. The older live 3110 server and all 47 formed records remained unchanged. No scan, protected company, decision, employee, task, wake, provider, DNS, domain, deployment, publication, spend, credential, or message was mutated. | `server/src/services/aether-portfolio.ts`, `server/src/services/aether-portfolio.test.ts`, `ui/src/pages/AetherPortfolio.test.tsx`, `work/aether-dns-canary-20260829/`, `FEATURE-PARITY.md`, `DESIGN.md` |
| 2026-08-30 | Canonical route classification stopped repository imports from becoming compulsory companies | Summon treated a repository as commercial intent, so research, personal, infrastructure, media, and archived work could inherit Core-8, buyer, checkout, revenue, and closest-to-cash requirements; MoneyMeta's optional $29 public layer was being mistaken for the project's identity | `company/AETHER_REPOSITORY_CLASSIFICATIONS.json` now validates one hard-to-vary route contract for every imported repository. The real Lightmark snapshot projects 47 classified, zero required, 26 commercial, and 21 noncommercial; the cash league contains only the same 26 commercial routes. MoneyMeta is `research_project` with Adam as beneficiary and `expose_one_output`, remains outside commercial formation/ranking, and exposes beneficiary-to-decision proof. The older `beeper.chat` scan identity aliases to canonical `cleared.chat`. Formation fails before mutation for unclassified or noncommercial selections. The `STATUS.md` verifier proves all 47 filenames and reports exactly 20 preserved noncommercial v1 files for a future route-contract migration; no bulk status structure was rewritten. The route-aware audit covers 68 connected repositories: 47 canonical imports pass classification and 21 additional directories stop safely at the gate. Thirty-four focused tests, shared/UI/server typechecks, and the production UI build pass. Token gates retain the inherited 50/22/8 baseline with no Aether Portfolio finding. A fresh isolated 3114/54329 instance applied all 199 migrations, returned health/auth ready with zero companies and an idle run phase, and served the exact real 47/26/21/0 contract. | `company/AETHER_REPOSITORY_CLASSIFICATIONS.json`, `packages/shared/src/types/aether-portfolio.ts`, `packages/shared/src/validators/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `ui/src/pages/AetherPortfolio.tsx`, `scripts/summon-audit-table.mjs`, `scripts/verify-summon-status.mjs`, `SUMMON_COMPANY_STANDARD.md`, `SUMMON_FORMATION_ROADMAP.md`, `FEATURE-PARITY.md` |
| 2026-08-30 | Application background tasks separated from Nightshift and implemented as provider-read deployment truth | The durable parity record incorrectly treated Polsia's application cron jobs as already covered by Summon Routines, Nightshift, and domain reconcilers, omitting a real founder outcome from the 47-row ledger | Summon now treats app jobs as source-defined and provider-deployed. Company Website resolves one exact secret binding and performs one fixed-origin GET for the connected Vercel project, requires returned project identity to match, validates and bounds definitions, redacts credentials, and projects only path, UTC schedule, optional host, and provider-disabled state behind `background_jobs.read`. The UI is explicitly read-only and separate from Nightshift; it has no run/edit/delete control and says Engineering plus an approved production deployment owns changes. Product Help sends app-cron questions to a dedicated Website guide rather than Routines. Twenty-six focused shared/provider/server/UI/help tests pass, Shared and UI typechecks and the production build pass; Server typecheck reaches only two inherited Recovery timestamp errors. Token gates remain 50/22/8 with no Company Website finding. The isolated heartbeat-disabled 3114/54329 runtime is restored healthy with zero companies and the unchanged 47/26/21/0 import contract; the HTTP-served production bundle contains the new Background tasks label and explicit Nightshift distinction. The parity ledger is now 48 outcomes: 26 match, 18 blocked by evidence, 4 not relevant, 0 partial, 0 missing. Live status remains blocked until one explicitly authorized disposable Vercel project proves a deployed definition and observed execution. No provider call, credential, cron execution, deployment, company/status-document mutation, activation, wake, message, publication, or spend occurred. | `server/src/services/vercel-background-jobs-provider.ts`, `server/src/services/company-website.ts`, `server/src/routes/company-website.ts`, `ui/src/pages/CompanyWebsite.tsx`, `ui/src/lib/summon-help.ts`, `packages/shared/src/validators/company-website.ts`, `doc/COMPANY-WEBSITE.md`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md`, `FEATURE-PARITY.md` |
| 2026-08-30 | Public Polsia parity-target drift became a deterministic check | A manual feature audit could confirm a point-in-time category count yet still miss a later guide or FAQ addition, recreating the omission that hid application background tasks | `pnpm check:polsia-parity` performs two bounded, fixed-origin, unauthenticated JSON reads of Polsia's public Help Center navigation and FAQ endpoints. It excludes drafts, validates and deduplicates identifiers, retains only guide slugs, FAQ IDs, and category counts, and fails when anything is added, removed, or reorganized. The metadata-only baseline contains no article or answer bodies. The live 2026-08-30 receipt matches 10 guide categories, 61 guides, 9 FAQ categories, and 48 FAQ entries with zero drift. Four Node tests cover exact match, draft exclusion, additions, removals, category changes, duplicates, and malformed identifiers. No authenticated browser, company, provider, credential, task, employee, message, deployment, publication, or spend was touched. | `scripts/check-polsia-parity-drift.mjs`, `scripts/check-polsia-parity-drift.test.mjs`, `doc/research/polsia-public-surface-baseline.json`, `package.json`, `FEATURE-PARITY.md` |
| 2026-08-30 | Multi-member Board Chat gained human speaker attribution | Summon already had company-scoped invites, roles, revocation, replay-safe acceptance, protected member removal, and an active-member directory, but Board Chat rendered every human author as the same unlabeled blue bubble; a shared company could not tell which board member gave direction | Board Chat now reads the existing company-scoped active-user directory. When more than one active human belongs to the company, persisted messages label the signed-in author as `You` and other authors by their company profile; optimistic local messages are also `You`. A single-founder company adds no redundant label. The invite boundary remains stricter than Polsia: random, single-use, 72-hour, board-copied URLs rather than a reusable multi-recipient bearer link, and Summon does not send a person-to-person invitation. Thirty-four focused invite, expiry, list, replay, directory, member-management, and chat tests pass; UI typecheck and production build pass. Token gates remain at the inherited 50/22/8 baseline with no Board Chat finding. No invitation was sent, no membership or company was changed, and no provider, credential, task, employee, message, deployment, publication, or spend was touched. | `ui/src/pages/BoardChat.tsx`, `ui/src/pages/BoardChat.test.tsx`, `server/src/routes/access.ts`, `server/src/__tests__/company-user-directory-route.test.ts`, `ui/src/pages/CompanyInvites.tsx`, `ui/src/pages/CompanyAccess.tsx`, `FEATURE-PARITY.md` |

| 2026-08-30 | Board Chat became a governed configured-Cofounder rail | Board Chat still used a hardcoded local Claude subprocess, a second assistant identity, local-only shell authority, and a browser-disconnect cancellation path even though Summon already had a provider-neutral governed employee runtime | One shared selector now fixes Cofounder choice for Company Loop, the server route, and both chat UIs: CEO first, then top-level, then oldest, while lifecycle failures remain explicit instead of silently falling back. The route rejects missing/pending/paused/terminated/invalid-chain employees without changing them, assigns and activity-logs the standing conversation, persists and logs the exact board comment, and issues one idempotent normal heartbeat wake with exact issue/comment/attachment context. Adapter, budget, company status, approval, workspace, retry, durable reply, attribution, and audit remain owned by the heartbeat service. SSE only observes status; disconnecting the browser does not cancel the accepted run. Active chat chrome shows the configured employee; static orientation copy says `Board Chat`; historical sentinel replies keep `Board Concierge`. Thirty-seven focused shared/Company Loop/server/UI tests pass. UI typecheck passes; server typecheck reaches only the two inherited Recovery timestamp errors with no Board Chat diagnostic. No real company, employee, issue, comment, wake, provider, credential, deployment, publication, message, approval, activation, or spend was touched. A fresh explicitly authorized disposable configured-employee file receipt remains pending. | `packages/shared/src/agent-eligibility.ts`, `server/src/services/company-loop.ts`, `server/src/routes/board-chat.ts`, `server/src/app.ts`, `server/src/__tests__/board-chat-route-feature-flag.test.ts`, `ui/src/pages/BoardChat.tsx`, `ui/src/pages/BoardChat.test.tsx`, `ui/src/pages/ChatMode.tsx`, `ui/src/lib/summon-help.ts`, `DESIGN.md`, `FEATURE-PARITY.md` |

| 2026-08-30 | Write-only Website application secrets became an exact-bound governed rail | Polsia's current first-party guide exposed a 49th useful founder outcome that Summon's 48-row ledger had omitted: add or change an App Secret, then deploy before the application uses it. Generic Company Secrets did not give the Website board a safe application-configuration path. | Migration `0201` adds one company- and Website-scoped environment-variable record whose public shape omits both secret ID and value. The board-only action validates an uppercase key and bounded targets, creates or rotates one encrypted company value, binds it to the exact environment-variable target, resolves the separate exact Vercel provider token, and performs one fixed-origin `sensitive` upsert. Success retains only allowlisted provider metadata and says a separate approved deploy or redeploy is required; ambiguous or failed completion records a sanitized error with `automaticReplayBlocked` and never retries itself. Twenty-nine focused shared/provider/server/UI/help tests pass. Shared, database, and UI typechecks, migration numbering/safety, Polsia parity-target drift, and the production UI build pass. Server typecheck reaches only the two inherited Recovery timestamp errors with no Website diagnostic. Token gates retain the inherited 50/22/8 baseline with no Company Website finding. The isolated heartbeat-disabled 3114/54329 runtime applied `0201`, returned healthy/auth ready with zero companies, served `/portfolio` with HTTP 200, and served the App Secrets, deploy/redeploy, and Background tasks controls. The canonical import verifier still reports 47 `STATUS.md` files, 26 commercial and 21 noncommercial classifications, zero unclassified, and exactly 20 preserved noncommercial v1 documents needing a future route-contract migration; none was structurally rewritten. The parity ledger is now 49 outcomes: 26 match, 19 blocked by evidence, 4 not relevant, 0 partial, 0 missing. No provider call, credential disclosure, environment write, deploy, company/status-document mutation, employee activation, wake, message, publication, or spend occurred. Live create/rotate/redeploy/readback remains blocked until Adam authorizes one disposable Vercel acceptance. | `packages/db/src/migrations/0201_company_website_environment_variables.sql`, `packages/db/src/schema/company_website.ts`, `packages/shared/src/types/company-website.ts`, `packages/shared/src/validators/company-website.ts`, `server/src/services/vercel-environment-provider.ts`, `server/src/services/company-website.ts`, `server/src/routes/company-website.ts`, `ui/src/pages/CompanyWebsite.tsx`, `ui/src/lib/summon-help.ts`, `doc/COMPANY-WEBSITE.md`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md`, `FEATURE-PARITY.md` |

| 2026-08-30 | Every current public Polsia help target became traceable to an evidence-honest Summon outcome, and customer billing gained a safe self-service source path | Counting public guide categories and FAQ entries could detect target drift but could not prove each individual target was represented in the parity ledger. The audit also exposed two omitted customer-account outcomes: self-service Summon billing and irreversible personal-account exit. | A metadata-only coverage manifest now maps all 61 public guide slugs and all 48 FAQ IDs to exact rows in the canonical parity ledger. The drift check rejects unmapped targets, duplicates, or invented outcomes and reports 51 traceable outcomes: 26 match, 20 blocked by evidence, 4 not relevant, 1 partial, and 0 missing. Summon now accepts only an exact Stripe-hosted no-code portal login URL from `SUMMON_BILLING_PORTAL_URL`, rejects credentials/query/fragment/alternate-origin data, exposes the setting only to a full board health response in authenticated mode, and renders Manage billing only when configured. The rail stores no Stripe secret, email, customer ID, or customer mapping and is explicitly separate from company budgets, provider subscriptions, and business customer payments. Personal-account export, deactivation, and deletion remain honestly partial rather than being conflated with company lifecycle. Twenty-three focused server tests, sixteen focused UI/help tests, and six parity-audit tests pass; UI typecheck and production build pass. Server typecheck reaches only the two inherited Recovery timestamp errors. Token gates retain the inherited 50/22/8 baseline with no billing-menu finding. The isolated heartbeat-disabled 3114/54329 preview is healthy, auth ready, serves `/portfolio` with HTTP 200, contains zero companies, and correctly omits the action because no real portal is configured. The canonical verifier still reports 47 `STATUS.md` files, 26 commercial and 21 noncommercial classifications, zero unclassified, and exactly 20 preserved noncommercial v1 documents needing a future route-contract migration; none was structurally rewritten. No live billing customer, provider call, credential, company, status document, employee, wake, message, deployment, publication, or spend was touched. Live receipt/payment-method/cancellation evidence remains pending an explicitly configured disposable-customer acceptance. | `doc/research/polsia-public-surface-coverage.json`, `scripts/check-polsia-parity-drift.mjs`, `server/src/billing-portal-url.ts`, `server/src/routes/health.ts`, `ui/src/components/SidebarAccountMenu.tsx`, `ui/src/lib/summon-help.ts`, `doc/ACCOUNT-BILLING.md`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md`, `FEATURE-PARITY.md` |
| 2026-08-30 | Personal-account lifecycle reached evidence-backed parity without conflating identity, company, and provider deletion | Summon could authenticate, sign out, edit a profile, and remove a company member, but it had no personal-data export, reversible account deactivation, irreversible identity deletion, or safe sole-owner reconciliation | Migration `0202` adds explicit active/deactivated/deleted identity state. Profile separates credential-free JSON export, deactivation, and permanent deletion. A Better Auth browser session less than 15 minutes old plus exact email/action confirmation protects destructive changes. Preflight blocks last active company owners and queued/running responsible work. Safe exit archives memberships, removes personal grants and instance authority, revokes board and responsible-agent keys, clears company defaults, unassigns open work, and pauses/disables responsible routines. Reactivation restores only identity; it never revives membership, grants, keys, or automation. Permanent deletion removes credentials, sessions, keys, and personal preferences, then replaces profile fields with a deterministic non-recoverable deleted-account sentinel while retaining opaque audit attribution. Company, provider, domain, deployment, subscription, customer subscription, and artifact lifecycles remain untouched. Three embedded-Postgres acceptances prove atomic last-owner blocking, no silent authority restoration, credential-free export, and pseudonymization; five focused account/auth/UI files pass 15 tests, and the broader seven-file auth/runtime slice passes 36. Shared/database/UI typechecks, migration numbering/safety, Polsia parity drift, and the production UI build pass. Server typecheck reaches only the two inherited Recovery timestamp errors. Token gates remain the inherited 50/22/8 baseline with no new file listed. The isolated heartbeat-disabled 3114/54329 preview applied only `0202`, restarted cleanly, reports health/auth ready, and remains empty. The ledger is now 27 match, 20 blocked by evidence, 4 not relevant, 0 partial, and 0 missing across 51 outcomes. The canonical verifier still reports all 47 `STATUS.md` files, 26 commercial and 21 noncommercial routes, zero unclassified, and exactly 20 preserved future route-contract migrations; no status structure was rewritten. No protected company, provider, credential, employee, task, wake, message, deployment, publication, subscription, or spend was touched. | `packages/db/src/migrations/0202_personal_account_lifecycle.sql`, `server/src/services/account-lifecycle.ts`, `server/src/routes/auth.ts`, `server/src/middleware/account-lifecycle.ts`, `ui/src/components/AccountLifecycleControls.tsx`, `ui/src/components/CloudAccessGate.tsx`, `doc/PERSONAL-ACCOUNT-LIFECYCLE.md`, `DESIGN.md`, `FEATURE-PARITY.md` |
| 2026-08-30 | Every remaining live-evidence gap became an explicit, machine-verifiable acceptance contract | The parity ledger correctly kept 20 outcomes blocked, but each row still described its receipt in prose. There was no one-to-one gate preventing an omitted row, undeclared provider mutation, real-customer test, incomplete cleanup, escaped artifact, stale hash, or credential-bearing receipt from being treated as parity proof. | `doc/research/polsia-acceptance-manifest.json` now maps all 20 blocked outcomes exactly once and declares authority, providers, allowed environment, disposable resource types, required evidence, allowed mutations, cleanup, and a hard boundary. `pnpm check:polsia-acceptance` reads the authoritative ledger and manifest without a provider call and reports all 20 as `awaiting_authorization`. `pnpm verify:polsia-acceptance` is intentionally red until all 20 have passing receipts. The receipt verifier requires an explicit authorization reference, safe resource IDs, allowlisted mutations, artifact paths contained under the receipt directory, exact SHA-256, redaction attestation, all required evidence kinds, and completed cleanup for a pass; it rejects credential-like fields and values. Failed authorized attempts may retain partial evidence but cannot become accepted. Six Node contract tests prove exact real-manifest coverage, honest missing state, full pass validation, credential rejection, mutation/cleanup rejection, and partial failed-attempt retention. No relevant provider credential exists in the current process environment, zero receipts are present, and no provider, customer, company, employee, task, wake, message, deployment, publication, subscription, or spend was touched. The outcome ledger remains honestly 27 match, 20 blocked by evidence, 4 not relevant, 0 partial, and 0 missing across 51 outcomes. | `doc/research/polsia-acceptance-manifest.json`, `scripts/check-polsia-acceptance-readiness.mjs`, `scripts/check-polsia-acceptance-readiness.test.mjs`, `doc/POLSIA-PARITY-ACCEPTANCE.md`, `package.json`, `FEATURE-PARITY.md` |

| 2026-08-31 | Closed the hidden paused-ad deletion gap without claiming live Meta proof | Polsia's current first-party ad guidance says an owner can permanently remove an ad only after pausing it so final spend is recorded. Summon's broad advertising row claimed the governed core but exposed only pause and resume; it could not complete the same cleanup outcome and its live acceptance manifest allowed only stopping spend. | Migration `0203`, shared states, receipts, and validator, the fixed-origin Meta provider, company service, board route, UI API, and Ads page now implement one paused-first permanent removal path. It requires exact `DELETE`, rereads final provider metrics, atomically reserves one UUIDv4, keeps final spend and audit history, treats exact 404 as already absent, makes repeated calls idempotent, quarantines transport, timeout, rate-limit, and server ambiguity, and lets Refresh reconcile by exact read without replay. Definite rejection restores paused state. The live acceptance manifest now requires disposable campaign deletion and provider-absence cleanup. Shared and database typechecks pass; shared validator tests are 5/5, provider tests 5/5, embedded Ads integration 1/1, and Ads UI tests 3/3. UI typecheck passes. Server typechecking reaches only the two inherited Recovery projection errors and reports no Ads error. No credential was resolved outside the encrypted embedded fixture and no Meta request, spend, publication, or external mutation occurred. The authoritative parity count remains 27 match, 20 blocked by live evidence, 4 not relevant, 0 partial, and 0 missing. | `packages/db/src/migrations/0203_company_ad_campaign_deletion.sql`, `packages/shared/src/types/company-ads.ts`, `packages/shared/src/validators/company-ads.ts`, `server/src/services/meta-company-ads-provider.ts`, `server/src/services/company-ads.ts`, `server/src/routes/company-ads.ts`, `ui/src/pages/CompanyAds.tsx`, `doc/COMPANY-ADS.md`, `doc/research/polsia-acceptance-manifest.json`, `FEATURE-PARITY.md` |
| 2026-08-31 | Polsia parity drift now detects changed promises under stable help targets | The existing monitor proved all 61 guide slugs and 48 FAQ IDs were mapped, but an article or answer could change behavior without changing its identifier. That blind spot allowed the current ad-deletion and company-name screening details to arrive beneath already-covered targets. | The bounded public audit now fetches each exact first-party article endpoint in batches of six, validates its returned slug and public state, normalizes Unicode, line endings, and trailing whitespace, and compares SHA-256 for title, category, summary, and body. It independently fingerprints every FAQ question plus sorted answer variants. The baseline stores only 109 identifiers and hashes, never Polsia article or answer bodies. Additions, removals, category changes, same-ID content edits, malformed responses, duplicates, missing articles, cross-origin redirects, and oversized articles all fail closed. Eight Node tests pass, including same-ID content drift and normalization. A live 2026-08-31 read-only audit matched 10 guide categories, 61 guides, 9 FAQ categories, 48 FAQ entries, 61 guide fingerprints, 48 FAQ fingerprints, and all 52 mapped parity outcomes. No authenticated browser, company, provider, credential, task, employee, message, deployment, publication, or spend was touched. | `scripts/check-polsia-parity-drift.mjs`, `scripts/check-polsia-parity-drift.test.mjs`, `doc/research/polsia-public-surface-baseline.json`, `doc/research/polsia-public-surface-coverage.json`, `FEATURE-PARITY.md` |
| 2026-08-31 | Public identity gained an evidence-honest pre-public trust and clearance gate | Current first-party Polsia FAQ answers screen potential brand conflicts and offer an official-domain review path, while Summon only reacted after publication through anonymous impersonation reports. Two differently punctuated public names could also race through independent requests, but copying a third-party “global clearance” claim would be unsafe without an authorized authoritative source. | Saving remains private. Publication now requires one connected Company Website with a provider-verified custom domain, a Unicode-normalized name without an existing public claimant under one transaction-scoped advisory lock, and a fresh board attestation to either a manual clearance search or qualified legal review. The activity receipt stores the selected basis; changing the name or review basis clears the UI confirmation. WIPO terms prohibit automated queries, bulk storage, and scraping; USPTO TSDR retrieves known cases rather than clearing names; official USPTO guidance says related goods/services, multiple sources, common-law use, and human judgment matter. Summon therefore links to manual official searches but never equates domain custody with trademark clearance or fabricates an automated legal verdict. Domain-verification loss still forces `noindex` and removes discovery. Four shared validator tests, five embedded server tests, and two focused UI tests pass; shared/UI typechecks and the production UI build pass. Server typecheck reaches only the two inherited Recovery projection errors. The live public-source audit maps all 61 guides and 48 FAQ entries to 52 outcomes. The ledger is now 27 match, 21 blocked by evidence, 4 not relevant, 0 partial, and 0 missing; the 21-row acceptance manifest includes an exact disposable public-identity receipt and currently reports all 21 awaiting authorization. Token gates remain at the inherited 50/22/8 baseline with no changed public-company file listed. The canonical verifier still reports 47 `STATUS.md` files, 26 commercial and 21 noncommercial routes, zero unclassified, and exactly 20 preserved future route-contract migrations; none was structurally rewritten. No protected company, real Website, provider, credential, DNS, domain, email, message, deployment, publication, or spend was touched. | `doc/research/PUBLIC-IDENTITY-CLEARANCE-2026-08-31.md`, `packages/shared/src/types/company-public.ts`, `packages/shared/src/validators/company-public.ts`, `server/src/routes/company-public.ts`, `server/src/services/company-public.ts`, `server/src/__tests__/company-public.test.ts`, `ui/src/api/companyPublic.ts`, `ui/src/pages/CompanyPublic.tsx`, `doc/PUBLIC-COMPANY.md`, `doc/research/polsia-acceptance-manifest.json`, `FEATURE-PARITY.md` |

## What changes the zero

Only one thing, and it is not code: **Anton has said yes and has never been sent
a link.** The give is finished, tested, and sitting in the repo. Everything in
this file is preparation for one message that has not gone out.

Order of operations that moves MRR off zero:

1. Adam sends the Anton give (ANTON_GIVE.md has the message and the diff).
2. On his reply, the founding offer with the two links in
   `outbound/anton-founding-send.md`.
3. Loop Haven, then send Michael (`outbound/michael-followup.md`).
4. Deploy the landing so the public rail is reachable by a stranger.

| 2026-08-31 | Three-path founder activation stopped hiding beneath generic company creation | Polsia's current first-party `gs-1` answer distinguishes Build my idea, Surprise me from the founder's background, and Existing business. Summon's parity row was marked match because it could create and import companies, but the actual onboarding exposed only a name-first generic path and did not preserve why the founder was starting. | The existing three-step wizard now begins with three original outcome choices. Build my idea requires enough problem context; Surprise Me requires founder background, offers an editable deterministic workspace name, and seeds a task to generate three candidates, reject two, and define one falsifiable test; Existing project preserves current context and asks for a binding-constraint diagnosis. All three seed a board-visible task even with no connected Claude/Codex fuel. Every task first routes the work across company, customer product, research, personal tool, portfolio infrastructure, media, or archive; buyer/offer/checkout/revenue gates apply only to commercial routes, while noncommercial work keeps beneficiary/question/evidence/decision/commercialization-policy proof. The task forbids deployment, publication, contact, spend, formation, or employee activation without separate authority. Four focused helper/component tests pass and UI typecheck passes. The Help Center now has 32 original guides and resolves “surprise me” to the exact activation surface. No company, issue, provider, credential, employee, wake, message, deployment, publication, or spend was created during verification; the 47 `STATUS.md` files and 20 future noncommercial route migrations remain untouched. | `ui/src/lib/onboarding-start-path.ts`, `ui/src/lib/onboarding-start-path.test.ts`, `ui/src/components/OnboardingWizard.tsx`, `ui/src/components/OnboardingWizard.start-path.test.tsx`, `ui/src/lib/summon-help.ts`, `FEATURE-PARITY.md` |

| 2026-08-31 | Permanent company deletion gained an honest backup preflight | Polsia FAQ `te-6` tells owners to choose database, codebase, and environment-file backups before deleting an account. Summon's lifecycle contract already preserved external code/provider resources and had portable Company Export plus real Stack database snapshots, but the destructive dialog only said “export anything you need.” It neither connected those exit paths nor warned that encrypted write-only secret values cannot be recovered. | The delete dialog now presents four explicit boundaries: portable Summon company package, applicable Stack database snapshot, owner-controlled source/workspace verification, and write-only secrets that must be rotated or re-homed because Summon never exports their values. The board must acknowledge all four before exact-name confirmation enables deletion; the UI does not claim that opening a link completed a backup. Twenty-three focused onboarding/lifecycle/help tests pass, UI typecheck and production build pass, the live metadata-only Polsia audit still matches 61 guides, 48 FAQ entries, and 52 outcomes, and the canonical verifier still reports 47 `STATUS.md` files, 26 commercial, 21 noncommercial, zero unclassified, and 20 future route migrations without rewriting them. Token gates remain the inherited 50/22/8 baseline with no changed lifecycle/help file listed. No company, export, snapshot, secret, provider, repository, workspace, subscription, employee, task, wake, message, deployment, publication, or spend was read or mutated. | `ui/src/components/CompanyLifecycleControls.tsx`, `ui/src/components/CompanyLifecycleControls.test.tsx`, `ui/src/lib/summon-help.ts`, `doc/COMPANY-LIFECYCLE.md`, `doc/research/POLSIA-FEATURE-PARITY-2026-08-25.md`, `FEATURE-PARITY.md` |

| 2026-08-31 | Summon product feedback and human-support boundaries corrected | Polsia FAQ `su-2` and `su-3` describe product bugs and feature requests, but the mapping treated them as company-work recovery and employee-output feedback. FAQ `su-1` promises a human while Summon's account menu still linked to Paperclip's feedback page, so neither privacy nor brand ownership was truthful. | Help now prepares reviewable copy-only bug and feature drafts with only redacted route, rounded viewport, broad browser/platform family, network state, and timestamp. It excludes query data, identifiers, logs, prompts, customer data, secrets, and files. The account menu no longer links to Paperclip. An operator may configure one vetted public HTTPS page through `SUMMON_SUPPORT_URL`; local or identity-bearing destinations fail closed, authenticated health exposes it only to the board, and the menu makes no human-support claim when absent. URL/health coverage passes 27 server assertions; four focused UI suites pass 28; UI typecheck and production build pass. Server typecheck reports no support diagnostic and reaches only the two inherited Recovery projection errors. The current first-party fingerprint audit matches 61 guides, 48 FAQs, and all 54 outcomes. The token gate remains the inherited 50/22/8 baseline with no changed feedback/support file listed. No destination was configured or opened; no report, request, ticket, email, upload, person-to-person message, provider call, task, company mutation, prioritization, deployment, publication, or spend occurred. Human support therefore remains blocked by evidence, bringing the honest ledger to 28 match, 22 blocked by evidence, 4 not relevant, 0 partial, and 0 missing across 54 outcomes. The 47 `STATUS.md` documents and 20 future noncommercial route migrations remain untouched. | `ui/src/pages/HelpCenter.tsx`, `ui/src/lib/product-bug-report.ts`, `ui/src/lib/summon-help.ts`, `ui/src/components/SidebarAccountMenu.tsx`, `server/src/support-url.ts`, `server/src/routes/health.ts`, `doc/PRODUCT-SUPPORT.md`, `doc/research/polsia-acceptance-manifest.json`, `FEATURE-PARITY.md` |

| 2026-08-31 | Company Stack source now hands off to governed iterative Engineering work | Polsia's current first-party app guidance describes an Engineering agent editing a private copy, committing each feature, and deploying it. Summon could create and publish the bounded initial app source and could separately govern deployment, but the published repository had no linked Project/Workspace for the accountable employee to continue through the normal runtime. | Migration `0204` links every successful or safely replayed application publication to one company-scoped Project and primary Git workspace pinned to the exact initial commit. `Open build project` leads to the normal issue board, so iteration inherits ordinary task, employee, adapter, budget, approval, checkout, and audit boundaries. Transaction locking and replay coverage prove one Project/workspace rather than duplicates. The durable receipt explicitly says private checkout still requires authorized Git credentials and that no task, wake, or production approval was created. The machine-verifiable live acceptance now requires the linked workspace, private checkout, one harmless governed build task, and exact iteration-commit readback. All 13 focused server/UI tests, shared/database/UI typechecks, migration numbering/safety, and the production UI build pass. Server typecheck adds no Stack diagnostic and stops only at the two inherited Recovery timestamp errors. No provider call, credential resolution, clone, task, employee wake, deployment, publication, protected company, or `STATUS.md` rewrite occurred; all 20 deferred noncommercial route migrations remain untouched. Live GitHub publication/private-checkout/iterative-task evidence is still required, so the exact parity ledger remains 28 match, 22 blocked by evidence, 4 not relevant, 0 partial, and 0 missing across 54 outcomes. | `packages/db/src/migrations/0204_company_stack_application_workspace.sql`, `packages/db/src/schema/company_stack.ts`, `packages/shared/src/types/company-stack.ts`, `server/src/services/company-stack.ts`, `server/src/__tests__/company-stack.test.ts`, `ui/src/pages/CompanyStack.tsx`, `ui/src/pages/CompanyStack.test.tsx`, `doc/COMPANY-STACK.md`, `doc/research/polsia-acceptance-manifest.json`, `scripts/check-polsia-acceptance-readiness.mjs`, `FEATURE-PARITY.md` |

| 2026-08-31 | Shapeable-informed Fleet command cockpit implemented and identically rescanned | The inherited cockpit leaked the Mission Control serif exception into ordinary app UI, distributed the primary truth across decorative cards, left a desktop dead zone, pushed fleet economics below Board focus on mobile, supplied no visible keyboard focus on cockpit actions, and used a 12-second boot guard that replaced the entire body and permanently deleted `#root` when a cold Vite boot was slow. | The local-only cure preserves Summon's black/white command-ledger identity while adopting Shapeable's transferable evidence structure: one binding profit truth, a counter-column explanation, a flat six-measure receipt, exactly three ordered board actions, a separate autonomy receipt, and a dense route-aware fleet ledger. Unproven revenue, expenses, cash, runway, and profit stay explicitly unproven. The boot fallback now writes inside the intact root so a late React boot can recover; a measured 14-second main-module delay rendered the fallback inside `#root`, then the eventual React boot removed it and recovered the cockpit with zero errors. Every cockpit action has a tokenized visible 2px keyboard outline. Helium rescanned the identical success profile at exact 1440x900 and 390x844 plus tablet, explicit dark mode, reduced motion, filter-empty, delayed loading, forced 500 error, and real Retry recovery: zero root overflow, zero visible controls below 44px, no unexpected console/page errors, body contrast 19.8:1 light and 17.68:1 dark, muted contrast 6.9:1 light and 7.85:1 dark, and zero active animations under reduced motion. Commercial/noncommercial filters returned 25/37 rows, search returned the exact `moneymeta.fun` row, and `/FLE/portfolio/ledger` resolved to the detailed ledger. The focused cockpit suite passes 2/2, UI typecheck passes, and the production UI build passes with inherited CSS/chunk warnings. Token gates remain the inherited 50 color / 22 arbitrary-value / 8 raw-size baseline and do not list `AetherPortfolioCockpit.tsx`. The private Shapeable auditor was not given the local page; its public surface and `llms.txt` were studied only as method and attribution. Isolated 3114 used one preview company, DB backups disabled, nine pending/paused employees, zero active employees, zero heartbeat-enabled employees, and zero running runs. The diagnosis action, populated-profit state, production performance, and assisted screen-reader behavior remain unmeasured. No employee activation, diagnosis run, provider call, credential, message, deployment, publication, spend, or protected runtime was touched. | `ui/src/pages/AetherPortfolioCockpit.tsx`, `ui/index.html`, `DESIGN.md`, `EVIDENCE.md` |

| 2026-09-01 | Fleet diagnosis became a truthful verbose operation and the landing cleared the Shapeable defect matrix locally | The Fleet action exposed only a generic pending state, so the board could not distinguish public-site auditing, cure-prompt generation, or ledger rebuilding. The public landing scored 49/100 in a fresh production Shapeable audit (`audit-20260831141736`): clipped logo SVGs, Vercel Insights console errors, motion that ignored reduced-motion preferences, undersized targets, motion over 400ms, and 12px text. | The run contract now records three exact subprocess stages with per-stage state, explanatory detail, current step, and timestamps. The cockpit renders them in an `aria-live` transcript across request, running, failure/retry, and success states. A real isolated 3114 run completed all three stages and returned 62 companies with no run error; Helium captured the truthful 2/3 intermediate state with zero browser errors. The existing landing copy was preserved while the seven measured defects were cured through static logo/EKG treatment, zero-duration reduced motion, larger navigation/footer/micro-note targets, 12px text raised to the overline token, and removal of the broken Insights include. Shapeable's identical local 390x844 and 1440x900 collector profile produced `audit-20260901034926`: 100/100, 100% coverage, 89% confidence, 21/21 checks measured, zero findings, zero prompts, zero console errors, and no horizontal overflow. Four focused UI tests and 17 focused server tests pass; shared and UI typechecks pass; server typecheck reaches only the two inherited Recovery timestamp errors. The token gate remains the inherited 50/22/8 baseline and does not list the changed cockpit files. The UI production build could not be reconfirmed because the Codex command stream aborted twice after Vite transforms; no build diagnostic implicated this change. The public site was not deployed and therefore remains at its recorded 49/100 production baseline until a separately authorized deployment. No protected runtime, company, employee, task, wake, provider, credential, message, publication, deployment, or spend was changed. | `packages/shared/src/types/aether-portfolio.ts`, `server/src/services/aether-portfolio.ts`, `server/src/services/aether-portfolio.test.ts`, `server/src/__tests__/aether-portfolio-cure-loop.test.ts`, `ui/src/pages/AetherPortfolioCockpit.tsx`, `ui/src/pages/AetherPortfolioCockpit.test.tsx`, `apps/landing/index.html`, `apps/landing/tokens.css`, `DESIGN.md`, local Shapeable audit `audit-20260901034926` |

| 2026-09-01 | Public Summon landing reached 100/100 on Shapeable's declared surface profile | The preceding receipt proved the cure only against an identical local matrix while the public URL remained at 49/100. Adam then performed the founder-only Vercel production deployment after the CLI settings fetch recovered and the repository's embedded-Postgres patch header plus lock hash were repaired. | Shapeable public audit `audit-20260901051130` measured `https://summon.company/` at 100/100 with profile `public-page-surface-v1`, 100% coverage, 89% confidence, 21/21 checks measured and passed, zero findings at 390x844 and 1440x900, zero horizontal overflow, and document sizes identical to the local deploy-ready receipt. This is a versioned initial-state surface score, not a complete UX score; journeys, full accessibility conformance, human task evidence, and eight listed principles remain unmeasured. The deployment was performed by Adam, not an agent. | Shapeable audit `audit-20260901051130`, `apps/landing/index.html`, `apps/landing/tokens.css`, `patches/embedded-postgres@18.1.0-beta.16.patch`, `pnpm-lock.yaml`, `EVIDENCE.md` |

| 2026-09-02 | Fleet became the first native Summon application shell instead of a Paperclip-skinned route | The portfolio still inherited Paperclip's immersive route, breadcrumb, banners, properties rail, feedback surface, undifferentiated navigation, and false “Summon did not finish loading” recovery. On a healthy local Vite cold compile, the 12-second guard reloaded the page before React could finish, restarting the same slow load. Mobile also placed six secondary measures before the board's three exact next actions. | The visible product boundary now has a permanent ink-and-ice Summon rail, Summon mark, Fleet-first navigation, one blue creation action, quiet company context, a single evidence canvas, and a dense company ledger. Paperclip protocol, API, database, and package compatibility remain intentionally intact underneath. Fleet excludes the generic breadcrumb, worktree/dev banners, properties panel, and feedback widget. The local boot guard waits 30 seconds, keeps `#root` intact, checks health without interrupting a healthy compile, and reloads automatically only after the runtime is first unavailable and later recovers. Mobile now orders profit truth, Board focus, then secondary metrics; at 380x844 Board focus begins at 430px, metrics begin at 1099px, root/main horizontal overflow is zero, and the boot fallback is absent. The final wide Helium capture has economics left, Board focus right, the Company fleet in the first viewport, and no document overflow. Forty-four focused tests pass across six files; a final two-file regression passes 13 tests; UI typecheck and two final production builds pass with inherited CSS/font/chunk warnings. Scoped whitespace is clean apart from line-ending notices. No company, employee, task, diagnosis, provider, credential, message, deployment, publication, spend, or protected runtime was mutated. | `ui/src/components/SummonMark.tsx`, `ui/src/components/Sidebar.tsx`, `ui/src/components/SidebarAccountMenu.tsx`, `ui/src/components/SidebarSection.tsx`, `ui/src/components/Layout.tsx`, `ui/src/pages/AetherPortfolioCockpit.tsx`, `ui/src/index.css`, `ui/index.html`, `ui/src/boot-guard.test.ts`, `DESIGN.md`, `C:/Users/adamp/Aether/helium-harness/summon-native-fleet-desktop-final-20260902.png`, `C:/Users/adamp/Aether/helium-harness/summon-native-fleet-mobile-final-20260902.png` |
| 2026-09-02 | Bounded free samples standardized across the MVP-ready commercial wedge | The exact isolated fleet contained 68 manifested repositories and 56 public sites, but the whole Aether fleet was not onboarded: 10 public sites remained unclassified or unmapped and 11 repositories were skipped. All 25 classified commercial products had exact Summon company links, yet there was no shared rule separating a useful sample from demo theater. Five ready products already exposed a real free experience, while beware.dog, deathmoney.fyi, and portal.voyage hid or visually subordinated theirs. Full unit cost was verified for 0 products, price clearance for 0, and mint readiness for 0 in the isolated snapshot. | `FREE_SAMPLE_STANDARD.md` now sets six MVP gates: real mechanism, keepable value, low friction, bounded cost, natural paid continuation, and truthful consent. Eight products qualify now. Result-specific, keyboard-visible, 44px-or-taller sample entry points were added to beware.dog, deathmoney.fyi, and portal.voyage without replacing their paid offers; the existing Lightmark, SellSniper, Skill Supply, Summon, and YouChop sample entry points were source-verified. `BUSINESS-MODEL.md` records Summon's bounded 48-hour diagnosis into the canonical $500 setup + $99/month founding company offer and rejects permanent freemium and per-seat pricing. All three changed sites return HTTP 200 with the CTA in rendered HTML and all three production builds pass. Focused lint passes for Death Money and Portal Voyage; Beware Dog's page compiles and typechecks in its production build, while direct ESLint is unavailable because that repository has no ESLint 9/10 flat config. The Helium visual pass was attempted but stopped at its one-time `Allow remote debugging?` permission prompt, so no screenshot, mobile geometry, or interaction claim is made. No checkout, Stripe object, message, diagnosis, customer data, deployment, publication, spend, protected runtime, or existing live site was mutated. | `FREE_SAMPLE_STANDARD.md`, `BUSINESS-MODEL.md`, `C:/Users/adamp/Aether/beware.dog/app/page.tsx`, `C:/Users/adamp/Aether/deathmoney.fyi/src/app/page.tsx`, `C:/Users/adamp/Aether/portal.voyage/app/page.tsx` |

## Rules

- A gate is not PASS without a number or a dated receipt.
- Self-payments and test charges do not count as stranger revenue.
- After every meaningful ship, add one row to the results log.
- Log zeros. Zero is the true number here and hiding it is lying.
